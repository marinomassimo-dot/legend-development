# REV-EXPOST-20260911-001 — ex-post hostile review of the 2026-09-10 → 11 landings

```
REVIEW_ID    REV-EXPOST-20260911-001
OBJECT       main, c1701be (exclusive) .. 6719e08 (object frozen at review start, 47 commits) — extended
             on the coordinator's instruction to d03aaa6 (+6: five HARNESS-SELFTEST-001 / orchestrator
             landings, two HARNESS-QUOTA-RESUME-001 landings). Every tool number below was re-derived
             at 6719e08 in a `cp -r` copy of the checkout carrying the gitignored `files/` tree.
LEVEL        R4 METHOD (Annex C.1) — harness and protocol changes, ex post under LEGEND_CORE §21e
REVIEWER     mirror (this actor wrote none of the object)
AUTHOR       plan (four sessions: HARNESS-SCREENS-001, -VALIDATOR-001, -ACQREC-001, -HANDOFF-001,
             -SELFTEST-001, -QUOTA-RESUME-001) and orchestrator
ADJUDICATOR  orchestrator
```

Read before the first act: `state_manifest_current.md` (READY), LEGEND_CORE §21c / §21d / §21e by
name, `roles/mirror.md` in full, Annex C, the retrospective § 5.2 and § 9, and `2026-09-10.md`.
Rules held: read-only toward everything but the two files this review lands; every experiment that
writes ran in `scratchpad/copy` or `scratchpad/copy2`, never in `/home/desktop/legend-development`.

---

## STEELMAN

The day did what the retrospective asked, in the order the retrospective ranked it, and it measured
before it built: the proposed prose grep was tried and discarded on its false-positive rate
(261/1458), the citation-only edge detector was tried and found to miss the container edge, the
census parser was found blind to the shape three actors actually used and fixed at the instrument,
and a wrong paper attribution in the specification (13 on 20530675) was corrected against the record
rather than followed. Every tool ships with a mutation matrix in its commit message; two matrices
found vacuous assertions rather than bugs and said so. The one incident of the night was restored
from `HEAD` with nothing unique lost, and it produced three separate controls within eight hours —
a tracked-file guard in the runner, a write-refusing tracer in the meta-test, and a `--guarded`
runner for mutation harnesses. The numbers that carry the operator's decisions reproduce: 81
manifests / 1458 locators / 17 candidates / 1 audited; 13 of 79 papers and 6 of 39 claims on a
flagged chain; 69 and 66 manifests on the two ratchets; 0 unresolved / 0 foreign over 96 queue
entries; 0/33 → 3/36 and 0/4 → 3/7 re-derived at the exact commits that produced them; 218 → 227
present with 0 mismatch; the release battery red on exactly `test_batch_queue` and
`test_surface_census`; `test_legend_handoff` 16/16; `self_test_coverage` both-false 0 of 50. This
is a harness that mostly says what it did, and that is rarer than it should be.

---

## EVIDENCE_FOR

Reproduced by running the tool that produced each number (copy pinned at 6719e08):

| Record claim | Command | Printed |
|---|---|---|
| § 9.1 — 17 candidates / 1458 locators, 1 with audit evidence | `locator_contradiction_audit.py` | `manifests=81 locator_entries=1458 … declared … 0/0 … candidates 17, of which carry audit evidence: 1 (structured 0, prose 1)` |
| § 9.6 — 0/33 → 3/36; § 9.7 — 0/4 → 3/7 | `attribution_census.py` in worktrees at `821b44c` and `cf27fc0` | `0/33 … 0/4` and `3/36 … 3/7` — exact. At HEAD: `5/38 … 6/10` |
| P1 — 13 of 79 papers, 6 of 39 claims | `dependency_integrity.py corpus --offline`; `claims --from-json` | `manifests with a DOI screened : 79 … >=1 flagged dependency: 13`; 9 chain lines over **6 distinct claims**, `claims in registry : 39` |
| § 9.5a — 0 unresolved / 0 foreign over 81 | `manifest_queue_id_crosscheck.py` | `manifests: 81 \| queue entries: 96 \| FT references checked 16 \| resolves and belongs: 16 \| unresolved: 0 \| foreign: 0 \| unchecked: 2` |
| § 2e — ratchet, 69 and 66 manifests, nothing blocks | `session_self_eval.py` | `[RATCHET] retraction_check.dependencies absent on 69 manifest(s)`; `[RATCHET] acquisition_recipe absent on 66 manifest(s)` |
| § 2e — 29 / 616 replayable, 587 NO_RECIPE with a reason | `reacquire.py status` | `recipe via back-fill 29 · NO_RECIPE, reason stated 587 · nothing stated 0 · replayable 29 / 616` |
| § 2e — 26 RECOVERED / 33914858 Cloudflare | `reacquire.py replay --with-recipe --limit 4 --no-write --no-record`; `--pmid 33914858 --ledger-only` | 3 RECOVERED + 1 `DIGEST_DIFFERS_SAME_ROUTE` (the PMC HTML of 15070730); `FAILED_HTTP_403_CLOUDFLARE PMID 33914858 [status=403]` |
| § 2e — evidence_presence 227 present, 0 mismatch | `evidence_presence.py` | `artifacts: 616 \| present: 227 \| absent: 389 \| digest_mismatch: 0` |
| § 2f — battery FAIL on the two declared reds, nothing else | `run_release_regressions.py` (copy, 5m35s) | `REGRESSION VERDICT: FAIL — test_batch_queue: exit 1, test_surface_census: exit 1`; no `TRACKED_FILES_WRITTEN_BY_SUITE` line; `test_legend_handoff` `Ran 16 tests OK`; `test_fulltext_receipts` `Ran 98 tests OK` (94 at H2 + the 4 CLI cases of 9535a1c) |
| § 9.3 — both-false 0 | `self_test_coverage.py --json` (5m35s) | 50 scripts · `entry_point_called` 44 · `real_artifact_case` true 35 / false 14 / skipped_declared 1 · **both-false 0** · refused 0 |
| § 9.2 — 7/7 verdicts carry a digest and refuse an uninformative pass | each screen on a zero-byte or absent input | 7/7 print `INSUFFICIENT_DATA` naming the missing input; `_refuse_suspect_surface('' , …)` raises `TypeError … must be os.PathLike` |
| 9.4 — `external_provenance` requires source and date | `deepdive_manifest.external_provenance_defect` | `{"source":"","date":…}` → "names no command or index"; bare `"recall"` → refused |
| Incident guard — the `--guarded` runner refuses the incident mutation | copy2, M6 applied, `self_test_coverage.py --guarded python3 test_dossier_quote_audit.py` | `GUARDED_WRITE_REFUSED open …/PMID15070730.md (mode w)`; 0 files modified; 0.6 s |

Mutation-matrix rows I re-ran and that held: `screen_verdict` M5 (path hashed as content → `TypeError`);
intrusion wiring P2 shape (`screen_file` on an empty path → INSUFFICIENT_DATA, exit 2);
`attribution_census` "returning SCREENED on an empty tree" (fixture root → `screened: files=1`).

## EVIDENCE_AGAINST

Ranked by what each would have let through.

### F1 — The incident is attributed, on `main`, by its author; the record still says UNATTRIBUTED, and the guard it credits could never have attributed it
**Files.** `learning/orchestrator/FINDING-20260911-DOSSIER-TRUNCATION.md:3` (`culprit UNATTRIBUTED`) and `:67` ("The guard attributes on the next run"); `2026-09-10.md` § 2c ("culprit unattributed after a bounded autopsy; guard shipped") — both unchanged at `d03aaa6`. Against them, on `main`: commit `1325494` (06:22:57, `plan`) — *"FINDING-20260911-DOSSIER-TRUNCATION, attributed: the writer was this task's own mutation matrix. Mutation M6 of dossier_quote_audit was `path.write_text('touched')` in the dossier loop and M3 of recapture_snippets made the dry run write"*; `framework/scripts/test_self_test_coverage.py:19–22` and `:130` (the incident, verbatim, as a fixture); the HARNESS-SELFTEST-001 ledger `INCIDENT_20260910_2226_ATTRIBUTED: SELF`.
**Forensics.** The coordinator's scratchpad (shared by its subagents) holds `mut_dqa.json` and `mut_rc.json` with mtime **22:26:13 UTC** — 27 s before the 22:26:40 overwrite — carrying exactly those two mutations, and `mutate_matrix.py` whose docstring says *"the first version ran the mutated tool straight through its real-artefact cases against the real root, and a mutation that made the tool write ran to completion."* The finding's grep of `.py` files could not find the literal because it lived in scratchpad JSON.
**Reproduced** (copy2, never the real tree): M6 applied to `dossier_quote_audit.py`, `python3 framework/scripts/test_dossier_quote_audit.py` run **directly** — the landed suite of `3dd1c71`, which "asserts the audit writes nothing" — printed `FAILED (failures=2)` after 755 s and left `modified under disease-models: 54 · dossiers now equal to 'touched': 54`. The suite detects the write after the bytes have landed. The same run under `--guarded`: refused, 0 modified.
**Why "the class is closed either way" is false.** (a) The runner's guard sees only suites *inside the battery*; the writer was a harness outside it, and the finding's own residual — "attributes on the next run" — describes a run that would never see it. (b) The runner's guard is detection after the fact (`tracked_state` before/after each suite): bytes land, then a line is printed. (c) It hashes **tracked** files only: `tracked_state` before/after creating `disease-models/wwox/NEW_dossier.md` in a fixture repo → `untracked new file under guarded tree detected: False`. (d) The `--guarded` tracer — the only control that *prevents* — wraps `builtins.open`, `io.open` and six `os.*` calls, and that is all: in a fixture root under `--guarded`, `sh -c "echo touched > <guarded>"` and `os.open(…, O_WRONLY|O_TRUNC)` **both landed bytes** (`artefact.json: touched`, `peer.md: touched`); only the `python -c open(…,'w')` subprocess was refused. `framework/scripts/fulltext_receipts.py` and `task_close.py` write through `os.open`; `task_close.py`, `legend_handoff.py`, `batch_commit.py` issue 15 git commands that write the working tree. A mutated ledger writer or a suite driving `git checkout --` is outside every guard the day built. (e) Neither the battery nor a scientist's direct suite run goes through `--guarded`; only `mutate_matrix.py` does, by convention.
**What it lets through.** Exactly the incident, again, from any route but the one that caused it — and an operator reading § 2c believes the class is closed and the culprit unknown, when a landed commit says who and how.

### F2 — The fourth R4 trigger is declared, not enforceable, and its measurement over-credits
**Path with no gate.** Copy, PMID 15070730 (complete receipt, 38 locators, every artefact present, strict validator PASS): `entries[0].proposition` reversed in place (`"NOT: …"`), `entries[1].contradicts_locator = "yes"` (a string). Then: `deepdive_manifest.py --verify-artifacts --require-current-schema` → **PASS, 0 gaps**; `locator_contradiction_audit.py` → `declared contradictions audited: 0/0` (a non-dict declaration is silently not a declaration); committed, `manifest_flag_drift.py --pmid 15070730` → **PASS**; `fulltext_receipts.py verify` → `OK: 156 chained receipt(s)`; `legend_lint.py .` → **PASS**. `deepdive_manifest.py` does not know the field (`grep contradicts_locator` hits only the audit tool, the brief and the skill). A reader who overwrites a persisted locator without declaring — the § 2.1 near-error's shape — meets nothing; a reader who declares badly meets nothing either.
**The measurement.** `locator_contradiction_audit.py:147–161` (`_entry_audit_evidence`) returns `STRUCTURED` when **any** entry key or **any manifest-level key** containing `audit` has a non-empty value. Fixture: entry with `audit_status: "NOT AUDITED — pending"` and a declared contradiction; second manifest with a top-level `figure_audit_table: "see dossier"` → `declared contradictions audited: 2/2`. Ten live entries already carry `audit_status: "AUDITED 2026-09-09 — blind locator audit, verdict applied"` — a 2026-09-09 audit of one triple will credit a later contradiction on the same entry as audited. Mutations G1 (manifest-level inference removed) and G2 (entry-level inference removed): `test_locator_contradiction_audit.py` **Ran 10 tests OK** both times; `--self-test 9 passed`. The behaviour the ratio depends on is pinned by nothing.
**What it lets through.** The one near-error class the day was named after, with a compliance ratio that reads 100 % once anyone declares.

### F3 — `legend_commit.sh` sweeps a peer's in-flight file when the pathspec is a directory
Fixture repo: peer edit to `peer.md`, my edit to `mine.md`, `bash scripts/legend_commit.sh "dir pathspec" disease-models/` → `2 files changed` — `peer.md` committed under my message. `git add -- <dir>` and `git commit -- <dir>` are directory-scoped, not file-scoped. `test_legend_commit.py` tests file paths only; the standing brief § 4 tells every scientist the wrapper "commits only the paths you name", and the wrapper's header calls itself "the mechanical half" of the § 4.3 authorship control. The mutation matrix of `047ccf9` did not include a directory pathspec.
**What it lets through.** The § 4.3 authorship incident, by the tool built to prevent it.

### F4 — `retraction_check.dependencies` does not bind to the paper it sits in
Copy: the block emitted by `dependency_integrity.py screen --pmid 18460020 --manifest-block` (`references_declared: 50`) pasted into `PMID16223882.json` (`multihop.references_enumerated: 26`), `citing_relation` filled with 20+ chars → `VERDICT: PASS — 0 gap(s)`. The validator refuses the placeholder relation (good, reproduced) and checks arithmetic within the block, but not that the block's declared/screened counts or DOIs belong to this manifest. This is the B7 shape — a manifest carrying another paper's record — in the slot built the same day the queue cross-check closed B7. Not in the 12-row matrix of `85091df`.

### F5 — `attribution_census.py` accepts arithmetic that cannot be true
Fixture root: `incidents: 3 / machine: 5 / severity_high: 1 / of which self: 4` → parsed as complete, `severity_high self-caught: 4/1`. No test in `test_attribution_census.py` pins consistency (`grep sum|add up|arithmetic` → none in the suite). The block is the number the operator invests by; a block that can say 4 of 1 is one the day's own `SCREEN_DIGEST_GATE` would refuse to call a verdict.

### F6 — Operator-facing numbers that do not re-derive, or claim more than the artefact holds
- `2026-09-10.md` § 1 row 9.3: **"26/46 called; both-false 13 → 10"**. The commit it cites (`2a26a25`) says **27/48, both-false 14**; the SELFTEST ledger says 49 scripts, 10 both-false at its start; a worktree at `2a26a25` re-derives **47 scripts / 28 called / 13 both-false** (no `files/`). Four denominators for one baseline; the record's is none of them. `SPECIFICATION_NUMBER_PROVENANCE_GATE`, landed the same afternoon, in the record that landed it.
- § 1 row 9.2 **"1/7 → 7/7 refuse an uninformative pass"**: at the verdict-record level, yes; at the exit-code level `genre_discriminator.py --artifact <empty>` exits **0** on `INSUFFICIENT_DATA`, and `erratum_scope_check.py --pmid <absent>` prints `examined: 0 … VERDICT: REPORTED` and exits **0**. A caller reading the status reads a pass (5/7 exit non-zero: intrusion check 2, `manifest_flag_drift` 2, `oa_status_dissent` 2, and the two library screens raise).
- § 2c **"the class is closed"** and the finding's `UNATTRIBUTED` — F1.
- § 2f "the verdict said exactly as much as it could and no more" is fair; but the same guard cannot see an untracked creation (F1c), which § 2c does not say.
- The standing brief v2 § 2 M2: *"Every screen in `framework/scripts/` now returns a record naming the digest"* — true of the seven converted; `erratum_scope_check --json` records carry `status`/`detail` per manifest and the corpus-level line `examined: 0` is not a digest-bearing refusal.
- `external_provenance_defect({"source":"PubMed search","date":"2026-13-45"})` → `None`: `ISO_DATE_RE` checks the shape, not the calendar. Low.
- P3 "4 undeclared containers → still 4": 2 manifests mention `.pptx`, 0 declare `supplement_binary`; the count of 4 was not re-derivable from the manifests alone (files, not manifests, may be the unit). Quoted, not verified.

### F7 — Two landings outside the dispatch: `d1e75a1`, `d03aaa6` (HARNESS-QUOTA-RESUME-001)
Briefly. They append a note to `state_manifest_current.md` § 7 (within the state-control carve-out), land two SLRs, an evidence JSON and a task ledger, and — outside the repository — set `autoContinueAtUsageLimit: true` in `/home/desktop/.claude/settings.json`, with a 0600 backup named in the record. Not on §21d's RESERVED list; the ledger's `WRITE_PERIMETER` is `null`, so the one write that matters was inside no declared perimeter and outside every guard the day built (the runner and the tracer see the repository, not `~/.claude`). The claims are hedged the way the day's own `MACHINE_FIELD_QUALIFICATION_GATE` asks: `CONFIGURATION_VERIFIED` and `REAL_RESUMPTION_VERIFIED` are separate keys, and the prose says *"Real resumption remains unverified; no periodic resumer added."* Finding, not objection: a write to the operator's user-level configuration by an agent deserves a perimeter line and a `DECISIONS_TAKEN` entry naming reversibility; the record has the backup and the revert instruction but not the perimeter.

## ALTERNATIVES_CONSIDERED

- *F1 is stale-record housekeeping, not a finding.* Rejected: the finding is the surface the operator is pointed to by § 2c, and its residual states a mechanism ("attributes on the next run") that the landed evidence shows is wrong. A record that contradicts a commit on `main` is the day's own `SPECIFICATION_NUMBER_PROVENANCE` shape.
- *The R4 trigger was always meant as method, not a gate; §21e says method is not ceremony.* Accepted as far as it goes — the commit says "reports a RATIO and never blocks". But the brief tells scientists the audit is "the fourth mandatory trigger" and the record's § 9.1 row reads as a control; a control whose only instrument counts `NOT AUDITED` as audited is not measuring compliance.
- *Directory pathspecs are user error.* Rejected: the wrapper is documented as the mechanical half of a control; a control that holds only when the actor is careful is the one that already failed (§ 4.3).
- *The `--guarded` tracer was never claimed to cover shell or `os.open`.* True of the commit message, which lists exactly what it wraps. The claim under review is § 2c's, that the class is closed.

## KEY_OBJECTIONS

1. The night's incident has an author, a mechanism and a reproduction, all on `main`, and the operator-facing record says none of the three.
2. Nothing the day landed prevents a write into the real tree from a suite run outside `--guarded`; two of the three controls detect after the bytes land, one of them is blind to untracked creations, and the preventing one is blind to `os.open` and non-Python subprocesses — routes the ledger writer and the git-driving tools use.
3. The fourth R4 trigger has no validator, no diff-based detector and an audit-evidence inference that no test pins; a silent in-place reversal of a persisted locator passes every gate, tool and lint in the repository.
4. Two new required/optional structures (dependency slot, census block) accept content that cannot be true of the record they sit in.

## VERDICT — per object class

| Object class | Verdict | Formulation where refined |
|---|---|---|
| Tool numbers asserted in `2026-09-10.md` § 1, § 2e, § 2f (audit, census, P1, ratchets, queue, replay, presence, battery, both-false) | **CONFIRMED** | reproduce by the producing tool; the two census baselines at their exact commits |
| § 1 row 9.3 baseline "26/46; 13 → 10" | **REFUTED** | not derivable from any commit or run; the commit says 27/48 · 14, a re-run says 47/28 · 13 |
| § 9.2 "7/7 refuse an uninformative pass" | **REFINED** | 7/7 at the verdict-record level; 5/7 at the exit-code level — `genre_discriminator` and `erratum_scope_check` exit 0 over nothing screened |
| § 9.1 fourth R4 trigger (Annex C.1 row, skill, reading modes, `locator_contradiction_audit.py`) | **REFINED** | "an act a declaring reader can make measurable; not enforceable, and the measurement counts any audit-ish key as an audit" |
| § 9.4 provenance WARN + `external_provenance` | **CONFIRMED** (minor: calendar not checked) | — |
| § 9.5a queue cross-check; § 9.5b intrusion wiring | **CONFIRMED** | corpus numbers reproduce; P2/N-shape mutations hold |
| § 9.6 census block + parser | **WEAKENED** | parser accepts impossible arithmetic; baselines reproduce |
| § 9.7 Annex A.1b / § 9.8 `INTERNAL_EDGES` + `lot_internal_edges.py` | **CONFIRMED** | `SHARED_AUTHOR` present (the retrospective asked for it); no reading contract issued since, so 1/15 contracts carry the block is not yet a measurement |
| P1 dependency screen; `retraction_check.dependencies` slot + ratchet | **REFINED** | "shape-checked, arithmetic-checked, ratchet non-blocking — and unbound to the paper: a foreign block passes" |
| P2 `acquisition_recipe`, `reacquire.py`, tracked `retrieval_manifest.jsonl`, PyMuPDF pin | **CONFIRMED** | status and sampled replay reproduce, including the predicted Cloudflare verdict |
| H2 `first_read` after reconstruction | **CONFIRMED** | 98/98; code reads as the commit describes |
| Commit wrapper `legend_commit.sh` + suite | **WEAKENED** | directory pathspec sweeps a peer's file |
| `screen_verdict.py` + seven conversions | **CONFIRMED** | M5 re-run holds |
| `self_test_coverage.py` + `--guarded` + 16 suite conversions | **REFINED** | "measures the axes it names; prevents writes through Python `open` and six `os` calls only; both-false 0 is real" |
| Release-runner tracked-file guard + `mtime_verdict` | **REFINED** | "names a battery suite that rewrote a tracked file, after the fact; blind to untracked creations and to writers outside the battery" |
| `FINDING-20260911-DOSSIER-TRUNCATION.md` (status, residual, "class closed") | **REFUTED** | culprit attributed on `main` by `1325494`; the residual's mechanism could not have attributed it; the class is open by three routes |
| `FINDING-20260910-HANDOFF-SUITE-RED.md` + `HARNESS-HANDOFF-001` | **CONFIRMED** | 16/16 on this host; diagnosis corrected against the fixture and left legible |
| Standing brief v2, four learned gates, dispatch obligations | **CONFIRMED** as routing; **WEAKENED** where the brief states a screen contract two screens do not meet at the exit code | — |
| `d1e75a1`, `d03aaa6` (quota resume) | **CONFIRMED** as records; **finding**: an out-of-repository write under a null perimeter | — |

## REVIEWER_CONFIDENCE / RESIDUAL_UNCERTAINTY / EVIDENCE_NEEDED

- Confidence **high** on F1–F3 (each reproduced in a scratch copy with the printed output above); **high** on the reproductions in EVIDENCE_FOR; **medium** on F6's "26/46" — I could not find the run that produced it, which is the finding, not proof that none existed.
- Residual: the scratchpad forensics attribute the 22:26 write to the mutation matrix by timing and content; the transcript itself was not read. `1325494`'s self-attribution and the reproduction make the alternative (a battery suite) unnecessary, not impossible.
- Evidence needed: none to act on F1–F5. For P3 (4 containers), a file-level count.

## WHAT_WOULD_CHANGE_MY_MIND

- F1: a run of the release battery at the incident's commit set that prints `TRACKED_FILES_WRITTEN_BY_SUITE` for a dossier — that would make a battery suite a co-writer. A tracer commit that wraps `os.open` and non-Python subprocesses, with the fixture in F1(d) refused.
- F2: a validator branch in `deepdive_manifest.py` that refuses a non-dict `contradicts_locator`, and either a diff-based detector (persisted locator text changed between two committed revisions of the same entry index with no declaration → candidate) or a receipt-level binding of `verbatim_locators` digest; and a test that turns red under G1/G2.
- F3: `test_legend_commit.py` with a directory pathspec that leaves the peer's file uncommitted.
- F4: the slot refusing a block whose `references_declared` differs from the manifest's own enumeration, or whose `reference_source` names another PMID.
- F6: the run whose output reads `26/46`.

## AUTHOR_RESPONSE

_(left empty for the orchestrator — Annex C.2: silence is not acceptance)_

---

## ATTRIBUTION_CENSUS
```
ATTRIBUTION_CENSUS
incidents: 4
machine: 2   blind_auditor: 0   peer: 0   self: 2
severity_high: 1   of which self: 1
undetected_known: 0
```
The four: exit codes captured from `head` instead of the tool (self, on re-reading the probe); the
coverage JSON parsed as a dict (machine, traceback); the `evidence_presence --json` keys guessed
(machine, traceback); `real_artifact_case` counted with `is True` on string values, reading as 0 of
50 (self — the number was implausible). The last is the severity-high one: reported, it would have
said the day's real-artefact axis was empty.

## DEFAULTS_TAKEN
- HEAD advanced during the review (`6719e08` → `f906d60` → `d03aaa6`) · froze the object at start, extended it on the coordinator's message · every number is tied to a stated commit · a moving object would have produced numbers nobody can re-derive.
- The dispatch names the battery command under `flock` in the real tree · ran it in a `cp -r` copy carrying `files/` · the object under review is a write into the real tree, and the copy reproduces the two declared reds exactly · a real-tree run would have risked the incident to prove the same line.
- Read-only tools were also run in the copy rather than the real tree · same reason; `evidence_presence` and the replay need `files/`, which the copy has.
- The coordinator's "resume from the start": kept the measured context (all of it at `6719e08`) and extended rather than re-ran · re-running 11 minutes of tools would have produced the same numbers.
- The in-flight `HARNESS-SELFTEST-001.json` was read for forensics though the dispatch says to ignore in-flight files · it carries the incident's self-attribution and is cited as such, not reviewed.
- Network replay limited to 5 artefacts, `--no-write --no-record` · enough to see all three verdict families; no external spend.
- Mutation G1/G2 and the incident reproduction were run in copies, tools restored by copy and checked byte-identical.

## STOP_LOG
Class 1: 0 · Class 2: 0 · Class 3: 0 — nothing waited on the operator.
