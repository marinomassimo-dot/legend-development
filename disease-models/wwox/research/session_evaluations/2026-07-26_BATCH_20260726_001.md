# Batch self-evaluation — BATCH_20260726_001 (2026-07-26)

> **Scope caveat, stated first.** This was a **propagation session, not a reading session.**
> No full text was read here. The content dimensions below are therefore **inherited** from
> the three upstream deep dives, and their own diagnoses live in
> [`2026-07-26_PMID22193544.md`](2026-07-26_PMID22193544.md) and
> [`2026-07-26_PMID35716775.md`](2026-07-26_PMID35716775.md). Grading inherited work as if
> this session had done it would be exactly the "answering yes to everything" failure the
> protocol warns about. What this session can be graded on is **conflict detection,
> propagation fidelity and verification** — and on those it is graded below.

## Scope and executable verdict

- **Batch/session:** `BATCH_20260726_001` — MANUAL trigger (3 candidates, below the 5-candidate
  threshold), propagating `CC-20260726-001/002/003` into the 4 canonical current files.
- **Studies and complete-read receipt IDs:** PMID 34214506 (`FTR-20260726-34214506-01`) ·
  PMID 35716775 (`FTR-20260726-35716775-02`) · PMID 22193544 (`FTR-20260726-22193544-01`,
  corrected by `-02` for output path only, no reread).
- **`session_self_eval.py`:** `PASS` — receipts 31, complete_fulltext_events 6,
  active_complete_reads 5, unread_premises 17/17 (at baseline, not above it). Two
  `[DECLARED GAP]` lines for multi-hop debt on PMID 34214506 and PMID 22193544 — honest,
  unclosed, visible.
- **Per-study manifest(s):** `deepdive_manifests/PMID22193544.json` validated as part of the
  receipt gate. PMID 34214506 and PMID 35716775 predate the manifest requirement in this
  workspace; no manifest was retrofitted, because a manifest written after the fact is
  testimony, not evidence.
- **Receipt verification:** `OK: 31 chained receipt(s), tail anchored in
  framework/state/state_manifest_current.md`.
- **Structural LINT:** pre-flight `PASS` (exit 0), post-propagation `PASS` (exit 0), one `INFO`
  (CLAIM 010, background-only, PAPER wikilink not required).
- **Local verdict:** `PASS`.
- **Workspace/global verdict and concurrent conditions:** `PASS`, but **not under clean
  conditions**. At least two other Claude Code sessions were live on this workspace during the
  day; one of them wrote to the research ledgers at 14:40–14:41 and appended the correcting
  receipt `FTR-20260726-22193544-02` **while this session was auditing the same defect**. The
  LINT verdict changed from `BLOCK_BATCH_COMMIT` to `PASS` between two consecutive runs of this
  session for reasons this session did not cause. No `PARALLEL_BRANCH` was declared by anyone.
  Propagation started only after 30 minutes with no writes anywhere in the tree.

## Content diagnosis

| Dimension | Evidence-backed answer | Grade | Debt or correction |
|---|---|---|---|
| Sequential full-text, figures, tables, supplements | **Not performed in this session.** Inherited: all three papers carry `complete_fulltext_read` receipts; PMID 22193544's receipt records figure-level *image* inspection (Fig 1–6 + 3 supplementary images), which is what made the Supplementary-Figure-A defect visible at all | inherited | This session verified the *receipts*, not the *reading* |
| Main message and original contribution | Preserved verbatim from the candidates into PAPER 054/055/056 and CLAIM 034/035, with figure/table anchors carried across (LFQ log2 values, Fig 9 n=2, Fig 3d/e L404A, Suppl Fig C GS-1) | strong | — |
| Hidden gold beyond keywords/abstract | Carried, not found here: the WWOX-negative title of PMID 34214506; the GS-1 result in a supplementary figure that killed the substrate-selectivity argument (DIS-009) | inherited | — |
| Source parity: context, type and recency | Preserved: a 2021 diabetic-retinopathy paper and a 2012 oncology-adjacent biochemistry paper both entered the canon on equal terms with WWOX-titled work | inherited | — |
| Team type, field density, observation vs interpretation | Preserved and **propagated as a live flag**: PAPER 056 carries the Chang N-S dispute note with an explicit statement of why it does *not* propagate to this paper's GSK3β biochemistry; field density (`WWOX AND GSK3` = 5 records) was written into CLAIM 016 as the reason consolidation is refused | strong | — |
| DATO / INFERENZA / IPOTESI / ESPANSIONE separation | Every propagated claim carries its tag; CLAIM 034 is explicitly `DATO (photoreceptor) + ESPANSIONE (any transfer)`, and its `Clinical meaning` opens by forbidding the "WWOX loss protects" reading | strong | — |
| Existing claims touched; conflicts/revival triggers | Three existing claims touched **at the place they live**: 009 (counter-directional note, explicitly *not* escalated to `conflicting evidence` because the studies are not comparable), 016 (mechanism + `PREMISE_TAG` on abundance-reports-activity), 024 (identified source, placeholder preserved) | strong | — |
| Multi-hop and corpus cross-query | **Not done here; declared debt carried forward** into the tracking log rather than left in prose: PMID 28123895, PMID 21444760, refs 38/39 of PMID 34214506, FT-022–FT-025 | partial | Two `[DECLARED GAP]` lines remain open by design |

## Persistence diagnosis

- **Durable ledger/registry IDs and wikilinks:** `PAPER 054/055/056`, `CLAIM 034/035`,
  `LIT-0402/0403/0404`, BLOCK 2 mirror rows 034/035, changelog row `WM_v3.2`, and a
  `BATCH_20260726_001 — tracking` section in the literature log. Ledger carve-outs
  (`DL-MECH-061/064/065/066`, `DL-BIO-012`, `DIS-008/009/010`, `D-14`, `FT-022`–`FT-025`) had
  already landed before the commit and were **not** re-created.
- **Reading queue/debt:** written into `full_text_queue_current.md` (FT-022–FT-025) and into
  the tracking-log batch section, not only into prose.
- **Dossier and commit candidate:** all three candidates marked `PROPAGATED` in place,
  append-only — none deleted, per the anti-pattern list.
- **Receipt source fingerprint, coverage and supplement state:** unchanged by this session; the
  ledger was **not** written to. The one correction that existed (`-02` on PMID 22193544) was
  made by the concurrent session through the validated writer, preserving the chain.
- **Can a future run distinguish full text from abstract only? How:** yes — each new PAPER
  record names its receipt ID inline in `Evidence depth`, and `coverage_report.py` joins the
  registry to the ledger. The receipt-declaration ratchet stayed at 21/21, which is the proof
  that none of the three new full-text declarations is unbacked.

## Process and capability diagnosis

- **Skills/gates/patterns used:** `legend-commit` (8-phase protocol), `legend-session-self-eval`,
  structural LINT pre- and post-propagation, receipt chain verification, the receipt-declaration
  ratchet, the `UNREAD_PREMISE` ratchet, link-target and release-gate suites, cardinality
  fixture.
- **Plausible skills deliberately declined, with reasons:** `legend-capability-scout` was **not**
  run as a literature/tooling radar — this session's capability gain came from a defect it hit
  itself, and inventing an unrelated scouting result would be decoration. `legend-deepdive` and
  `find-fulltext` declined: propagating is not re-reading, and re-reading three papers to
  re-verify a commit would have produced receipts without new coverage.
- **Failures, retries, extraction mismatches or concurrency events:** four, all recorded.
  **(1)** Three lineage references in the candidates were **wrong**: `CORPUS P376` and
  `CORPUS P263` are indices of the seed TSV, but the registry already uses those exact IDs for
  *different publications* (Farooq 2015 PMID 25662954; Chang 2014 PMID 25537520). Propagated as
  written, the commit would have asserted a false identity between two pairs of papers.
  Rejected in Phase 2 and documented in the records themselves. **(2)** CC-002's conditional
  merge of `CORPUS P204` into PMID 35716775 was **not** executed, because its stated condition
  ("if the batch lineage confirms") was never satisfied; the placeholder is preserved unmerged.
  **(3)** CC-001 proposed ledger entries in an ID scheme this repository does not use
  (`DISC-2026-07-26-A/B/C`, `DISM-2026-07-26-A`); the real entries had already landed as
  `DL-MECH-061` / `DIS-008`, so the claim wikilinks point at the real IDs. **(4)** Concurrency:
  see the executable verdict above.
- **What caught each failure before an overclaim:** (1) and (2) were caught by **reading the
  target records instead of trusting the candidate's prose** — no tool would have flagged them,
  because a plausible-looking ID that exists is invisible to a link checker. (3) was caught by
  grepping the ledgers for the proposed IDs before writing the wikilinks. (4) was caught by the
  **concurrent session**, not by this one: it appended the correcting receipt, and this session
  observed the LINT verdict flip underneath it. That attribution matters — this session was
  auditing the defect, the other session fixed it.
- **Disease-agnostic micro-upgrade shipped:** **the mandatory backup no longer fails the release
  checks.** Phase 3 of `prompt_batch_commit.md` requires a snapshot of the canonical files under
  `backup/` before any canonical write. Because the snapshot is a byte-identical copy,
  `test_link_targets.py` reported *every* canonical record as "resolves to 2 files" and
  `public_release_gate.py` returned `BROKEN_MARKDOWN_LINK` on the copies' relative links — i.e.
  **obeying the protocol broke the gate**, which is a direct incentive to skip the backup. Fixed
  by excluding `backup/` in both tools, at
  [`scripts/test_link_targets.py`](../../../../scripts/test_link_targets.py) and
  [`scripts/public_release_gate.py`](../../../../scripts/public_release_gate.py), each with the
  reason written where the exclusion lives. Disease-agnostic: it is framework tooling.
- **Regression/evidence that makes the upgrade persistent:**
  `test_batch_commit_snapshot_directory_is_not_scanned` in
  [`scripts/test_public_release_gate.py`](../../../../scripts/test_public_release_gate.py) —
  builds a snapshot tree with a dangling relative link and asserts the gate stays silent. Proof
  beyond the unit test: the snapshot was restored to `backup/snap_20260726_1600` and both suites
  re-run green with it present.
- **Residual risk and next decisive action.** Three, ordered. **(1) The commit trusts the
  candidates on the science.** This session verified identity, structure, links and receipts —
  it did not re-derive a single experimental value from the papers. A candidate that misread a
  figure would have propagated. That is inherent to batching, but it should be named rather than
  hidden by a green verdict. **(2) Concurrency is still unguarded.** `active_parallel_branches:
  none` in the manifest was false for most of today, and nothing executable contradicted it. The
  next decisive action is a session lockfile checked at start — the collision has now happened
  twice in one day, on the same paper. **(3) The multi-hop debt is real:** FT-022 (the
  neuron-specific GSK3β2 isoform) may qualify TX-005's safety reading, and it is queued, not
  read.

## Grades, separated

- **Content propagated:** strong — but inherited, and explicitly graded as inherited.
- **Process of this session:** partial. It caught three lineage defects that no check could
  have caught, and it shipped the fix for the defect it hit. It also ran a batch commit in a
  workspace with undeclared concurrent writers, which the protocol lists as an ABORT condition
  in its pre-flight table. It proceeded on a 30-minute quiescence check and the operator's
  explicit instruction; that is a judgement call, and it is recorded here as one rather than
  presented as compliance.

**Not medical advice.** Nothing propagated in this batch is a therapeutic recommendation.
