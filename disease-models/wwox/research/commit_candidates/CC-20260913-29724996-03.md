# CC-20260913-29724996-03 — `LIT-0096` still describes a paper nobody has screened, and it disagrees with `PAPER 091`

**Author:** `orchestrator` · task `ORCH-BATCH-PREP-20260913` · 2026-09-13
**Source:** PMID 29724996 · Abu-Remaileh et al. 2018 · *Cell Death & Disease* 9:511 · PMC5938702
**Receipt:** `FTR-20260909-29724996-01` (`complete_fulltext_read`). **No new reading and no new receipt.**
Every value below is copied from a canonical record that already exists, or from the deposit's front matter.
**Change class:** **MINOR**, bookkeeping. It fills placeholder fields and removes one field conflict.
No claim changes status, no scientific value moves, and nothing is added to what the model asserts.
**Status:** queued; **no canonical file modified**.
**Target WM:** `target_wm_version`: the version the consuming `BATCH_COMMIT` achieves (current at batch time: `WM_v4.4`).
This candidate touches no claim, no working-model line and no meta file, so it neither requires nor forbids a bump.
Rebase is required if `PAPER 091` or `LIT-0096` changes before the batch.
**Review floor:** R1, mechanical. `legend-locator-audit` is **not triggered**: no claim is touched.
**Relation to other candidates:** independent of `CC-20260912-29724996-02`, which edits `PAPER 091`'s `Note`.
No field this candidate reads from is a field that candidate rewrites, so the two can be applied in either order.

**Nothing here is medical advice.** The disease model is the reference genotype class; no individual.

---

## 1 · The defect

`BATCH_20260909_001` promoted `CORPUS-STUB-073` to `PAPER 091` and declared `LIT-0096` «completed»
(`CC-20260909-29724996-01` § BATCH DISPOSITION). The status and depth lines of `LIT-0096` were updated.
The descriptive fields were not.

The accepted M4b comparison note, version 4 (sha256 `62a9939f…`), § 5, lists the defect with its criterion.
A field is stale if its value is `not yet extracted`, `unknown`, `not yet screened`, `unassigned`, `none` or `none yet`
while the record is `processed — complete_fulltext_read`. Under that criterion **nine** fields are stale.
**One** more field is not a placeholder but contradicts `PAPER 091`.
The independent re-examination of version 4 reproduced both counts (finding F6).

This candidate re-derived the list from `literature_tracking_log_current.md` at `main` `a69034b`.
It is the same list, at the same lines.

## 2 · What this candidate does not do

- It does not reopen the accepted note, and it carries nothing from the note except the list of fields.
- It does not touch `Short title` («corpus paper 73»), `Discovery *` or `Report mentions`.
  Those fields are outside the verified criterion. Widening the edit would make this candidate larger than what was verified.
- It does not touch `CLAIM 025`'s unresolved source identifier (`CORPUS P191`, `Identifier: PENDING`).
  That is an identity question, not a copy, and it has no verified answer.
- It does not touch the four other processed LIT records that also still carry `not yet extracted`.
  They belong to other papers and are outside this case.

---

## 3 · The four mandatory blocks

### FILES TO CREATE

None.

### FILES TO UPDATE

1. `disease-models/wwox/registries/literature_tracking_log_current.md`, record `## LIT-0096`.
   Ten field values are replaced. Every other line of the record stays byte-identical.
   Line numbers are at `main` `a69034b` and are given for location only. Match on the FROM text.

| line | field | FROM | TO | TO copied from |
|---|---|---|---|---|
| 3000 | `**Authors:**` | `not yet extracted` | `Abu-Remaileh M, Khalaileh A, Pikarsky E, Aqeilan RI` | deposit XML front matter, 4 `contrib` names; the same list as `CC-20260909-29724996-01` § 1 |
| 3001 | `**Year:**` | `unknown` | `2018` | deposit XML; `PAPER 091` identifier DOI |
| 3002 | `**Source type:**` | `not yet screened` | `primary experimental (mouse conditional knockout, in vivo)` | `paper_registry_current.md:6962` |
| 3003 | `**Journal/source:**` | `not yet extracted` | `Cell Death & Disease 9:511` | deposit XML `journal-title`, `volume`, `fpage` |
| 3012 | `**Primary pathway:**` | `unassigned` | `P5 — metabolism / HIF1α–glycolysis` | `paper_registry_current.md:6963` |
| 3013 | `**Genotype/model tag:**` | `unassigned` | `` `Wwox^ΔHep` (Alb-Cre × Wwox^fl/fl), DEN-induced HCC, ± high-fat diet — not a WWOX-DEE allele `` | `paper_registry_current.md:6964` |
| 3014 | `**Transferability:**` | `unassigned` | `T3` | `paper_registry_current.md:6965` |
| 3015 | `**clinical relevance:**` | `MED` | `INDIRECT` | `paper_registry_current.md:6966` — the conflict |
| 3016 | `**Claim links:**` | `none` | `CLAIM 025 (qualifying evidence; no new claim)` | `paper_registry_current.md:6967` |
| 3017 | `**Working Model impact:**` | `none yet` | `none — no working-model block is redefined by this record` | wording of `LIT-0410`; `working_model_current.md` carries 0 occurrences of this PMID or of `PAPER 091` |

2. This file: `## BATCH DISPOSITION` appended by the integrator, append-only.
3. `framework/state/state_manifest_current.md`: `last_batch_commit_*` fields, by the batch procedure.

### FILES UNCHANGED

`paper_registry_current.md` is the source of the values and is not edited by this candidate.
`claim_registry_current.md` is unchanged, because `CLAIM 025` already links `PAPER 091` in its evidence boundary.
`working_model_current.md`, every `meta_*_current.md`, the discovery ledger and the receipt ledger are unchanged.

### CHANGE LOG

`LIT-0096` · ten descriptive fields · nine placeholders replaced by the values already on `PAPER 091` or in the deposit.
`clinical relevance` moves from `MED` to `INDIRECT`, so the tracking log agrees with the identity record.
Claims touched: 0. Scientific values moved: 0. Direction of change: none, bookkeeping only.
Origin of the defect: `BATCH_20260909_001` marked the record completed without filling its descriptive fields.

## 4 · How the integrator checks this candidate

Each TO value is a copy, so the check is a comparison and needs no reading.

```bash
sed -n 6962,6967p disease-models/wwox/registries/paper_registry_current.md
sed -n 2998,3021p disease-models/wwox/registries/literature_tracking_log_current.md
grep -c "29724996\|PAPER 091" disease-models/wwox/registries/working_model_current.md   # expect 0
```

The author list and journal come from `files/fulltext/PMID29724996_AbuRemaileh2018_PMC.xml`.
That directory is gitignored. Where it is absent, the same four names stand in `CC-20260909-29724996-01` § 1,
which `BATCH_20260909_001` integrated.

---

## BATCH DISPOSITION — appended by the integrator, append-only

**Status:** **PROPAGATED** — `BATCH_20260913_001 (VPS batch, never on main)` (2026-09-13, ACTOR_ID `orchestrator`).

`LIT-0096`: all ten fields applied as written. The Mirror consultation verified every value at source, field by field, closing the gap that the earlier verification covered only the count of stale fields.

---

## BATCH DISPOSITION — appended by the integrator, append-only

**Status:** **RE-QUEUED** — recovered 2026-09-26 from the VPS backup (`06ee25a`). The VPS batch that disposed of this candidate never reached `main`: re-queued for `BATCH_20260926_ALDAZ`. Identifiers written on the VPS are annotated in place as `(VPS numbering)` / `(VPS batch, never on main)`; full-text queue ids were renumbered (see `disease-models/wwox/research/vps_recovery_20260925/README.md`).
