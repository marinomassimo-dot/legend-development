# CC-20260909-25331887-01 — **HELD. NOT PROMOTABLE.** SI Appendix of Abu-Odeh 2014: WW1-vs-SDR domain assignment for the ATM axis

**Author:** `scientist-a` · task `AQEILAN-FT-A-001` (wave 2) · 2026-09-09
**Source:** PMID 25331887 · Abu-Odeh et al. 2014 · *PNAS* 111(44):E4716–E4725 · PMC4226089
**Receipt:** `FTR-20260909-25331887-02` (`partial_fulltext_read`, `inadequate_prior_coverage`, prior `FTR-20260810-25331887-01`)
**Manifest:** `deepdive_manifests/PMID25331887.json` — 39 locators; strict validator **FAIL on 7 paths only** (unrecoverable figure crops, §4)
**Change class:** would be **MINOR** — **but no change is proposed by this document.**

---

## 🔴 0 · Why this candidate is HELD, and what that means

**The R4 review floor was not satisfied for the ten supplementary locators this candidate would
rest on.** A blind locator audit was dispatched twice on 2026-09-09. The first auditor stalled at
its prompt record and never returned a verdict to this session; the synchronous rerun was
terminated by a session rate limit before producing output. The coordinator reports that an auditor
did complete and returned **SUPPORTED on four triples and OVERSHOOT on six**, with replacement
wording — **but this session does not hold those verdicts.**

Reconstructing an audit from memory is exactly the act the audit exists to prevent, and shipping
six known-overshooting propositions as though corrected is exactly the failure the floor exists to
catch. So:

- every supplementary proposition in the manifest carries an **`[UNAUDITED]`** prefix and
  `audit_status: PENDING_AUDIT_CORRECTION`;
- **this candidate proposes no claim edit and must not be promoted;**
- the next session holding the verdicts applies them, then rewrites this file as a live candidate.

**What is NOT in doubt:** the SI Appendix was genuinely retrieved and all six pages genuinely
inspected as rendered images; the artifacts are fingerprinted; the ten attestations describe pixels
that were looked at. What is unverified is whether each **proposition** says more than its page
supports — and reportedly six do.

## 1 · What the reading did establish (recorded, not promoted)

The finding this candidate would have carried, stated here **only** as the thing awaiting audit:

> **Fig. S4A maps the WWOX–ATM interaction to the WW1 domain and not to the SDR domain**, and
> **Fig. S4B** maps the ATM side to ~1530–2100 aa. If severity in WWOX-DEE tracks the **SDR** domain
> (`CLAIM 030`), the DNA-damage arm of WWOX biology would be **dissociable** from the alleles that
> dominate WWOX-DEE severity — and **P47T, a WW1 allele that abolishes PPxY binding (`CLAIM 007`),
> gives the MILD phenotype**, so loss of the WWOX–ATM interaction would not be sufficient for severe
> neurological disease.

That inference is attractive, which is precisely why it must not be promoted on unaudited locators.

Also recorded in the dossier and awaiting the same audit: **K274R retains a substantial ubiquitin
ladder** (K274 is not the sole acceptor); the **K63-linkage-specific panel is the faintest of four
antibodies**; the **cycloheximide chase is a single non-monotonic experiment** whose wild-type
control rises to 1.4 at 4 h under blocked synthesis; and **two captions flatten differences their
panels show** (Fig. S3B persistence at 24/48 h; Fig. S5 MDC1 retained while p-CHK2 is abolished).

## 2 · Claims this would touch, when audited

`CLAIM 007`, `CLAIM 023`, `CLAIM 024` (consolidated baseline) and `CLAIM 029`, `CLAIM 030`
(in observation). **None is edited by this document.**

## 3 · What the wave DID close, independent of the audit

- **The declared supplement debt is paid.** `FTR-20260810-25331887-01` recorded
  `supplementary: unavailable`; the SI Appendix is now retrieved, fingerprinted and read.
- **The prior reading is re-established as verifiable.** This checkout held **0 of 9** declared
  artifacts. The article PDF re-derives **byte-identical** (`42df070a…`). PMC no longer serves
  byte-identical HTML, but **all 17 prior text locators re-verify character-for-character** against
  today's re-fetch. The prior reading is sound; only its container changed.
- **The M0 adjudication.** The dispatch's `inadequate_prior_coverage` came from the prior receipt's
  **depth label**; its coverage **map** has no `not_read` and is admissible for
  `complete_fulltext_read`. The body was therefore not re-derived — only the one surface the map
  itself names as missing.

## 4 · Declared debt — an environment gap, not a reading gap

The strict validator **FAILS on exactly seven paths**: the figure PNGs extracted **by xref with
PyMuPDF**. This deployment has **no `fitz` and no `pip`**, and poppler does not reproduce the bytes.
They are **kept declared** — deleting them for a green verdict would erase the record of what the
prior reading inspected. `fitz` is referenced by **ten** repository scripts including
`deepdive_manifest.py` and `regenerate_adjudications.py`, so **rule 5e's own page-adjudication
remedy is unrunnable in this deployment**. Reported to the orchestrator as a harness gap.

**Not medical advice.**
