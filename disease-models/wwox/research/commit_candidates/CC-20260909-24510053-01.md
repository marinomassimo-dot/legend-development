# CC-20260909-24510053-01 — a Tier C "background corpus only" placeholder that carried an active rejection's missing provenance

- **Actor:** `scientist-a` · task `AQEILAN-FT-A-001` wave 3 · 2026-09-09
- **Source:** PMID **24510053**, Gardenswartz A, **Aqeilan RI** (corresponding), *Exp Biol Med (Maywood)* 2014;239(3):**253–263**, DOI `10.1177/1535370213519213`, **no PMCID**
- **Receipt:** `FTR-20260909-24510053-01` · `complete_fulltext_read` · `first_read`
- **Manifest:** `deepdive_manifests/PMID24510053.json` — strict PASS, 0 gaps, **9 locators, all page adjudications**
- **Dossier:** `fulltext_dossiers/PMID24510053.md`
- **Change class:** **ORDINARY.** `target_wm_version`: unchanged. **No consolidated baseline claim is narrowed, reversed, corroborated or removed.**
- **Source type:** 🔴 **REVIEW.** Secondary throughout. No claim gains or loses evidentiary support from this reading.
- **Review:** R4 `legend-locator-audit` **not triggered, and the trigger was checked rather than assumed** — see §5.

---

## 1 · Proposed for the canonical record — a registry promotion

`CORPUS P308` / `LIT-0308` currently reads:

```
Tier (FASE 1): C
Status: screened — corpus placeholder
clinical relevance: LOW
Claim links: none — triage only
Role: background corpus only
Note: FASE 1 triage 221–400 — no deep-dive performed.
```

**Proposed:** promote to a read record carrying `full text reviewed`, backed by the receipt above,
with `Source type: Review` kept prominent and `Role` rewritten from *"background corpus only"* to
*"read secondary source; carries the primary provenance for DIS-001's ACK1 cascade"*.

**`clinical relevance` should stay LOW** and is proposed unchanged. That is not an oversight: the
paper genuinely contains nothing about the reference genotype (§3), and inflating the field because
the reading turned out useful would corrupt exactly the signal the field exists to carry. **Its
value is mechanistic and archival, not clinical.**

**No new claim is proposed.**

---

## 2 · 🔴 The finding — `DIS-001`, provenance supplied, verdict unchanged

Active rejection **`DIS-001`** («Inhibiting ACK1/ITCH will not rescue Q230P», status **REOPENED**)
declares its load-bearing premise as *"that this route ends in the proteasome"*, tagged
`PREMISE: DEFAULT_FROM_TEXTBOOK` → **`D-01`** (*polyubiquitination → proteasome*), whose recorded
cost is *"Discarded a testable ACK1/ITCH cascade for days."*

This review does **two** things to that entry, and they pull in opposite directions:

| | |
|---|---|
| **Supplies** | The primary provenance `DIS-001` was missing: the ACK1→WWOX polyubiquitination cascade was published as **ref 31 = PMID 16288044** (Mahajan NP, Whang YE, Mohler JL, Earp HS, *Cancer Res* 2005;65:10514–23), whose `fulltext_receipts.py status` returns an **empty list** — this corpus has never opened it. |
| **Is** | Itself a fresh instance of `D-01`: it defines ubiquitination as labelling *"and thus designated for degradation via the proteasomal system"* — a flat universal, **no linkage determination anywhere in the paper**, cited to ref 30, a *bone-formation* review with no WWOX data. |
| **Resolves** | **Nothing about linkage.** |

**Proposed disposition — `DIS-001`: STRENGTHENED IN PROVENANCE, UNCHANGED IN VERDICT.** It stays
reopened. The reading that could actually move it is PMID 16288044, and this is not that reading.

**Proposed for `D-01`:** append this review as a citable in-the-wild instance of the default,
occurring in a WWOX-specific source by the gene's own primary group.

### 2b · A dated negative that must not travel undated

> *"So far no ubiquitin E3 ligase was associated with WWOX under normal or disease states."*

A **2014 snapshot, already superseded** — the corpus's own ACK1/ITCH thread names **ITCH**, an E3
ligase, in the WWOX P252A context. Proposed to be recorded **with its date bound to it** and its
`REVIVAL_TRIGGER` marked *satisfied on arrival*. A negative of this shape carried forward without
its date is precisely the silent, self-reinforcing false negative `epistemic_discipline.md` §2
exists to prevent.

---

## 3 · Transfer verdict — negative, and measured

Screened over the **38,845-character body with the 103-item reference list separated off first**:
**zero** occurrences of `epilepsy`, `seizure`, `brain`, `CNS`, `WOREE`, `encephalopathy`, `ataxia`,
`intellectual`, `recessive`, `syndrome`, `Q230`, `SCAR12`. The only two `neuro` hits in the file are
**journal names in the bibliography**; separating body from back matter before screening is what
kept them out of the finding — the contamination class registered as **open item 3** of the receipt
contract. Sole `germline` mention: two germline mutations in a somatic lung-cancer series.

- Clinical/phenotypic transfer: **NONE**
- Mechanistic transfer: **`INFERENZA`, narrow** — WWOX has a *regulated* degradation route via a
  tyrosine kinase, which is the transferable structure for a model whose central problem is normal
  transcript with absent protein
- Epistemic class: **`ESPANSIONE`** for the cancer census; **`DATO`** only for *what the review reports*

---

## 4 · Queue movement proposed

**PMID 16288044 (Mahajan 2005) — raise priority.** Already in `full_text_queue_current.md`; empty
receipt status; it is the primary behind an active reopened rejection; and `WWOX AND Ack1` returns
**four PubMed records in total** (measured 2026-09-09). This candidate does not read it and does not
pre-judge it.

---

## 5 · Why R4 was not run, stated rather than passed over

`legend-locator-audit` is mandatory when a reading narrows, reverses, corroborates or removes a
`consolidated baseline` claim, or justifies a MAJOR bump. **This reading does none of those.** Its
dispositions are a registry promotion and a dismissal-ledger re-audit that strengthens provenance
without altering a verdict. If PMID 16288044 is later read and moves `DIS-001`, **that** reading
carries the audit.

---

## 6 · Two corrections to dispatch metadata

- First author is **Gardenswartz**, not Abdeen (Abdeen SK is ref 14, *Oncogene* 2011).
- Pages are **253–263**, not 253–9.

---

## 7 · Acquisition and surface — recorded because both are reusable facts

**Five OA indexes were unanimous and wrong.** NCBI idconv (*"not found in PMC"*), Europe PMC
(`isOpenAccess N`), Unpaywall (`closed`, zero locations), OpenAlex (`closed`) and Semantic Scholar
(`CLOSED`) all key on the **SAGE DOI**; the journal has migrated to **Frontiers** and the article is
free and complete at `ebm-journal.org`. **Crossref's publisher field is the only structured signal
that shows it.** Worth generalising: *a closed verdict from every DOI-keyed OA index is not evidence
of a paywall when the journal has changed publisher.*

**No structured surface exists, and the PDF text layer is `SUSPECT`** — 12 `þ` for `+`, 8 `0x02` for
`−`, all twenty inside genotype or receptor-status notation (`Wwox (C3H)+/−`, `WWOX+/+`, `ER−`). It
was **refused, never hand-corrected**. All nine locators are rule 5e page adjudications;
`regenerate_adjudications.py verify` reports **9/9 digests regenerated and 9/9 needles resolved
inside their crops**. This is the **first reading in this deployment to use that remedy**, runnable
only since PyMuPDF was installed today on this actor's wave-2 `tool_preflight.py` finding.

---

## 8 · The reason this candidate exists at all

The registry said Tier C, *"background corpus only"*, clinical relevance LOW, *"no deep-dive
performed"*. That placeholder held the primary provenance of an **active reopened rejection**.
`gold_is_in_the_details` rule 1 — *no tier, score or category authorizes NOT reading* — worked in
this lot rather than being quoted from the file.

---

## BATCH DISPOSITION — appended by the integrator, append-only

**Status:** **INTEGRATED** — `BATCH_20260909_001` (2026-09-09, ACTOR_ID `integrator`).

CORPUS P308 promoted to a read record in place, Role rewritten, pages corrected to 253-263, clinical relevance deliberately left LOW; LIT-0308 completed. The DIS-001 / D-01 provenance work is ledger-level and was not written into the four current files.
