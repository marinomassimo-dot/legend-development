# Orchestration record — 2026-09-10 (improving the Scientists, unattended continuation)

Operator directive, morning: restart from the problems the Aqeilan sweep exposed
([`2026-09-09_actor_retrospective.md`](2026-09-09_actor_retrospective.md)) and improve the
Scientists, using the saved census of comparable repositories
([`2026-09-09_sweep_capability_comparison.md`](../../../../governance/candidates/2026-09-09_sweep_capability_comparison.md)).
Operator directive, afternoon: continue unattended for some hours; check that every aspect
has been taken as deep as it goes.

## 1 · What the morning landed, and what it measured

The retrospective's decisive number: on the six near-errors one step from the record, machines
caught **zero**; the reader 3, blind auditors 2, the coordinator 1. Investment followed that.

| § | Change | Baseline → now |
|---|---|---|
| 9.1 | Contradicting a persisted locator is an R4 trigger; measured by `locator_contradiction_audit.py` | 17 candidates / 1458 locators, 1 with audit evidence |
| 9.2 | Every screen reports `screened{digest,bytes}` and refuses an uninformative pass (`screen_verdict.py`) | 0/7 → **7/7**, 1/7 → **7/7** |
| 9.3 | Meta-test: does a self-test call its own entry point on a real artefact (`self_test_coverage.py`) | unmeasured → 26/46 called; both-false 13 → **10** |
| 9.4 | Identifier/count provenance wired as WARN + `external_provenance` | 19 undeclared, not retro-edited |
| 9.5a | Queue-ID cross-check | 0 unresolved / 0 foreign over 81 manifests; real B7 draft as fixture |
| 9.5b | Intrusion check wired into the validator | 0 added false positives over 50 live intrusions |
| 9.6 | `ATTRIBUTION_CENSUS` required and parsed (`attribution_census.py`) | 0/33 → 3/36; severity-high self-caught **3/8** |
| 9.7 | `DEFAULTS_TAKEN` / `STOP_LOG` in the task contract (Annex A.1b) | 0/4 → 3/7 |
| 9.8 | `INTERNAL_EDGES` resolved at M0 (`lot_internal_edges.py`) | 0/3 contracts; the 4 known edges reproduced + 2 |
| P1 | Dependency-integrity screen (`dependency_integrity.py`) | per-PMID 1 → **13 of 79** papers; **6 of 39 claims** on a flagged chain |
| P3 | `supplement_binary` kind, `.pptx` declarable | 4 undeclared containers → still 4 (declaring them is a reading act) |
| H2 | First contact after a `legacy_reconstruction` is writable | 94/94 |
| — | Commit wrapper versioned with the regression that would have caught its defect | — |

Four things in the specification were wrong and were corrected against the record rather than
followed: the proposed prose grep (261/1458 hits — noise), a citation-only edge detector (3 of 4,
looking complete), the § 9.5(b) paper attribution (the 13 belongs to 21731849), and the census
parser's own blindness to single-wave contracts (the instrument, not the actors). One licence
field in a pin asserted what its own run could not verify; fixed at the generator.

Release battery: `FAIL` on exactly the three inherited reds — `test_batch_queue` (ratchet, operator's
`BATCH_COMMIT`), `test_surface_census` (gitignored evidence), `test_legend_handoff` (proven red at
`c1701be`, diagnosed in [`FINDING-20260910-HANDOFF-SUITE-RED.md`](../../../../learning/orchestrator/FINDING-20260910-HANDOFF-SUITE-RED.md)).
Nothing green before is red after.

## 2 · What is still open, honestly, and where each goes

| Item | Why it is still open | Disposition |
|---|---|---|
| **P2** acquisition recipe | The biggest census item, 6–10 h, and this checkout is the live case: 0 of 616 declared artefacts are versioned, and the repository changed hosts yesterday | **dispatched**, `HARNESS-ACQREC-001`, owns `deepdive_manifest.py` |
| Dependency screen as a manifest slot | P1 is a report; a reading should know at reading time whether its reagents descend from flagged work (S7) | **dispatched** inside `HARNESS-ACQREC-001` as its first, small deliverable — same file, same shape of change |
| `test_legend_handoff` red | Harness, T0, and the transport that moved this repository | **dispatched**, `HARNESS-HANDOFF-001` |
| 10 scripts blind on both self-test axes | Peer-owned during the morning; nobody's now | **dispatched**, `HARNESS-SELFTEST-001`, excludes `deepdive_manifest.py` |
| The Scientist standing brief | It is what the readers are graded against, and every change above post-dates it | **orchestrator**, this afternoon: promoted from a dated dispatch artefact into a versioned protocol |
| Learned gates from today | Three failure shapes recurred across actors and none is in the registry | **orchestrator** |
| Stricter `first_read` rule | Deliberately not shipped with the H2 fix. Measured today: **0** live events would violate it, so it is free of history; it still breaks four `rechain` fixtures | recorded here; a separate decision |
| Declaring the 4 `.pptx` containers; queueing the 13-paper / 6-claim reading debt | Reading acts on other actors' readings and on scientific surfaces | **left to the next scientist wave**, named in the standing brief so it is found |
| Four receipt-schema decisions (§4.1, §4.1b, §4.3, §4.3b) | Epistemic semantics — what "complete" may mean — referred by the actors, not improvised | **operator** |
| Ex-post Mirror review of the day | §21e: Mirror reviews landed changes after the fact | **orchestrator dispatches at the end**, after the three streams close |

## 2b · Landed by the orchestrator while the three streams ran

| Change | Commit | What it closes |
|---|---|---|
| `scientist_standing_brief.md` v2 — the dispatch-ready brief as a versioned protocol, carrying every rule the sweep changed; v1 marked superseded, not rewritten | `0a54c58` | § 4.5 stale brief; the readers are graded against a file that cannot lag the controls |
| Four learned gates: `SCREEN_DIGEST_GATE`, `INVISIBLE_COMPLIANCE_GATE`, `SPECIFICATION_NUMBER_PROVENANCE_GATE`, `MACHINE_FIELD_QUALIFICATION_GATE` | `d38131c` | three failure shapes that recurred across actors today and were in no registry |
| `roles/orchestrator.md` — seven dispatch obligations, each with its incident | `7801879` | § 4.5 false acquisition hint; the record's own number quoted into a task |
| `legend-capability-scout` — environment preflight, verdict keyed to its host | `7801879` | census § 4.2: `fitz` "absent" on one host, 1.28.2 on this one |
| `test_fulltext_receipts.py` — four CLI-level cases through the real subprocess | `9535a1c` | the writer actors run on every reading had `entry_point_called: false` |

Measured and **not** shipped: the stricter `first_read` rule (refuse it after any reading of the
article) would be violated by **0** live receipts, so it is free of history; it still turns four
`rechain` fixtures red and stays a separate decision.

## 2c · Night of 2026-09-10 → 11: the three streams died, and something wrote the dossiers

All three afternoon streams were terminated by a rate limit before reporting — the
retrospective's I1, again — with substantial uncommitted work intact in the checkout. And at
22:26:40 UTC, while two of them ran the release battery **concurrently** as their baseline, **54
tracked dossiers were overwritten with the word `touched`**. Restored from `HEAD`; nothing unique
lost; culprit unattributed after a bounded autopsy; guard shipped — the runner now names any suite
that writes a tracked file under a guarded tree and fails on it. Record:
[`FINDING-20260911-DOSSIER-TRUNCATION.md`](../../../../learning/orchestrator/FINDING-20260911-DOSSIER-TRUNCATION.md).

`DEFAULTS_TAKEN` · the streams were **resumed two at a time, not three**, because three at once is
what the limit killed; the third follows when one closes · batteries run under an exclusive lock
from now on · a non-empty `git status disease-models/` before a commit is a stop-and-report, never
a restore, so the attribution is not destroyed by tidiness.

## 3 · Rules of this round

Same as the morning: exclusive file ownership per stream, path-scoped commits through
`scripts/legend_commit.sh`, baseline before change, mutation matrix in every commit message,
`DEFAULTS_TAKEN` and `STOP_LOG` in the task JSON, `ATTRIBUTION_CENSUS` in the closing report, no
push, no spend, no `BATCH_COMMIT`, no change to §21c/d/e. A number quoted from a specification
is re-derived before it becomes a target — the morning's lesson, in the afternoon's dispatch.
