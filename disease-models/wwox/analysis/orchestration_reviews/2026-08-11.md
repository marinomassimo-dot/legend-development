# Orchestration review — 2026-08-11 (Mirror, round 1)

Opened because a measurement found something, not because the date changed. Read-only; every number
below is printed with the command that produced it, and every count with its complement.

Yardstick: `main@415bb71`, `framework/scripts/*` as they stand there, artifacts from the shared checkout.

---

## 1. Diagnosis

1. **`main` holds a 25-locator manifest whose receipt exists on no merged branch, and every gate is
   green.** `PMID29724996.json` names `FTR-20260810-29724996-01`; that receipt lives only on
   `lettore-b`, which is unmerged. A merge carried the manifest without the ledger entry that attests it.
2. **Yesterday's collision repair was correct in the ledger and incomplete outside it.** The renumbering
   that resolved `FTR-20260810-42422765-02` re-pointed a back-reference nobody updated: `PMID42422765.json`
   on `main` says `receipt: …-04`, and the receipt that claims it is `…-05`.
3. **The cause of both is one missing direction.** `session_self_eval` verifies *receipt → output* and
   says so in its own verdict — *"every declared output resolves"*. **Nothing verifies output → receipt.**
   5 of 34 manifests on `main` fail the reverse direction while every check passes.

---

## 2. Material observations

### O1 — The reverse direction, measured corpus-wide (OBSERVATION, high confidence)

Partition over **all 34** manifests on `main`: does `manifest.receipt` appear in that receipt's `outputs`?

```
  ok: 29     not ok: 5

  PMID29724996   receipt=FTR-20260810-29724996-01   RECEIPT ID NOT IN THE LEDGER
  PMID42422765   receipt=FTR-20260810-42422765-04   claimed instead by FTR-20260810-42422765-05
  PMID22193544   receipt=FTR-20260726-22193544-02   claimed by nobody
  PMID34214506   receipt=FTR-20260726-34214506-02   claimed by nobody
  PMID35716775   receipt=FTR-20260726-35716775-02   claimed by nobody
```

*Command:* load the ledger, walk every manifest, check membership of the manifest's own path in the named
receipt's `outputs`; complement (the 29) counted and reported.
*Concurrent state of the gates:* `legend_lint` **PASS** · `session_self_eval` **PASS — "every complete
read has landed and every declared output resolves"** · 84 receipts, 38 complete events.

Three sub-classes, and they are not equally severe:

| # | manifest | class | severity |
|---|---|---|---|
| 1 | `PMID29724996` | the named receipt exists on **no merged branch** — `grep` over `main`, `lettore`, `evidence-index`, `codex/*` returns 0; it is on `lettore-b` alone | **the manifest is on `main`, its attestation is not** |
| 2 | `PMID42422765` | off-by-one created by yesterday's renumbering | a real reference pointing one receipt too early, at a superseded partial |
| 3 | three 2026-07-26 manifests | their receipts predate manifests being listed as outputs | legacy, and honest to leave — but currently indistinguishable from 1 and 2 |

### O2 — Sub-class 1 is the mirror of a rule already written (OBSERVATION)

`CLAUDE.md` states it from one side: *a branch carries the manifest; it does not carry the evidence.*
This is the same hazard from the other: **a merge carried the manifest and left the ledger entry
behind.** The rule exists, at one site, in one direction. Same shape as every other finding of
2026-08-10 — the pattern applied unevenly rather than misunderstood.

### O3 — Sub-class 2 is the seventh collision class, paid (OBSERVATION, closes a 2026-08-10 prediction)

The `FTR-20260810-42422765-02` collision I recorded yesterday is **resolved on `main`, and resolved
correctly in the ledger**: A's receipts were renumbered 02→03, 03→04, 04→05, and re-parented so that
A's first continuation chains to `-01` rather than to Codex's supplement reading. That is exactly the
repair the collision required.

What was not carried: `PMID42422765.json` still names `-04`. **Renumbering an identifier repairs the
ledger and silently re-points every reference held outside it** — the *position/identity* class, at a
new site. `lettore` still holds the un-renumbered chain, so whoever merges it must take `main`'s
numbering and fix the manifest in the same commit; `git merge-tree main lettore` → 3 conflicted files,
the ledger among them.

### O4 — The triggers I left open (measurement, no finding)

| trigger | state |
|---|---|
| the contract freeze | **held** — `deepdive_manifest.py` unchanged since 22:37 on 08-10 |
| `provenance` / `readout` shipped | **no** — `grep -cE '"(provenance\|readout)"'` on `main` → 0. §6.1's interval stays **untested** |
| `validation_context` shipped | **no** — 0 |
| `lettore-b` landed | **no** — my 2026-08-10 review still blocks `BATCH_COMMIT` on `38499540`, by design |
| `codex/pmid-34831305` landed | **yes**, and `codex/pmid-42422765-s8` with it |

---

## 3. Proposal

**Check the reverse direction, in the tool that already loads both sides.**
`session_self_eval` reads the ledger and walks the manifests today; it verifies *receipt → output* and
stops. The addition is the membership test in O1's command and nothing else: for every manifest, the
receipt it names must exist **and** must list that manifest among its outputs.

*Falsifiable:* run it now — it must report exactly the five above, no more and no fewer. Run it after the
repair — zero, and it must **stay** zero across the next merge that renumbers anything.
*Trade-off, and it is real:* the three legacy manifests will fail on day one for a reason that is not a
defect. Either they are grandfathered by a ratchet — the `non_increasing` shape `growth_anchors`
already implements, so no new mechanism — or the check ships noisy and is ignored within a week, which
is the failure mode this repository has already paid for.

**Not proposed:** a new script. The data is already in one process; a second tool would be the third
site of a pattern that exists twice.

---

## 4. The negative — what I looked for and did not find

- **A sixth broken back-reference.** 29 of 34 are clean, and the 29 were counted, not assumed.
- **Any gate that already catches this.** `legend_lint` PASS, `session_self_eval` PASS, `growth_anchors`
  measured; `build_evidence_index.py:159` reads `manifest.get("receipt")` but only to index it.
- **Contract movement overnight.** Zero commits to `deepdive_manifest.py` since 22:37. The freeze held
  for the first stretch since it was proposed — recorded as an observation, not a vindication: three
  hours of a quiet night is not evidence about a rule.
- **A second renumbering casualty.** Only `PMID42422765` references the renumbered range from outside
  the ledger; `-02`, `-03` and `-05` are named only by `reading_state.md`, a session evaluation, the
  protocol and the test suite, all of which are on `main` and internally consistent.
- **`analysis_at` collisions (2026-08-10 item 13).** Still open, still nobody's. Not measured today
  because it needs an answer from Codex, whose budget is exhausted.

---

## 5. 09:00 — half of §1 resolved itself in forty-five minutes, and that is the finding about me

Re-run of O1's partition on `main` at 09:00, unchanged command:

```
REVERSE DIRECTION on main — 36 manifests (was 34)
  ok: 32   not ok: 4

  PMID42422765   receipt=...-04   claimed by ...-05        STILL OPEN
  PMID22193544 · PMID34214506 · PMID35716775               legacy, unchanged
```

**`PMID29724996` is gone from the list.** `main` absorbed `lettore-b` up to `b296699` (08:23) and the
receipt travelled with it. The 18-needle debt of §3 item 4 is **paid**: `main`'s 36 manifests now carry
**4** blocks, all `schema_version 1`, against the ten green-on-branch/red-at-merge manifests measured
last night. A merged `main` at 08:28 and is now 390 bytes behind rather than 23 700.

**None of it happened because I reported it.** The merge was in flight while I wrote §1 at 08:15 and
dispatched at 08:20. **My most urgent finding of the morning had a lifetime of about forty-five minutes
and was closed by work I had no part in.**

🔴 **The lesson is about *what* I choose to measure, not how.** I have written twice that a rate over
live branches is stale on arrival. This is that rule one level up: **at this cadence, measuring the
current state of `main` and reporting it is racing work already in flight.** What survives the race is
the structural claim — that nothing verifies `manifest → receipt` — not the count and not the roster.

§3's proposal stands unchanged; §3 item 4 is **withdrawn as already paid**. The dispatch list I sent at
08:20 was two items stale by 09:00. I did tell the Orchestrator to re-run the numbers before acting —
and I put it last, where it reads as a courtesy rather than as the load-bearing instruction it was.

**What does not move, and is what I should have led with:**

| | |
|---|---|
| `manifest → receipt` is unverified | still true at 09:00 — 4 instances, `legend_lint` PASS, `session_self_eval` PASS |
| an identifier renumbered in the ledger silently re-points references held outside it | still true — `PMID42422765` is the standing example and the **only** item of my dispatch list still open |
| a branch's validator can be older than the rule that judges it at merge | mechanism unchanged; today's instance closed because A merged `main`, not because anything prevents it |

---

## 6. A stale *number* and a stale *dispatch* are two classes, and we produced one each in the same hour

State verified independently at 09:03, item for item against the Orchestrator's own re-run:

```
main 7101ac4 (08:54) "Merge lettore-b: three collisions of one shape, and the hash chain decided the third"
36 manifests · ledger 87 receipts · reverse direction not ok: 4 — the same four
lettore 10 ahead · lettore-b 0 · evidence-index 0
```

Two instances, one hour, independent, and neither actor knew the other was doing it:

| | what was measured | what was sent | what was already true |
|---|---|---|---|
| **Mirror** | `main` at 08:15 | a five-item dispatch at 08:20 | items 1 and 4 closed by the merge at 08:23 |
| **Orchestrator** | `main` appearing static | a rebuke to Plan for building the echo guard *instead of* merging | `7101ac4` was committed at **08:54:59**, before the message. Plan was merging |

**This is not the stale-number class, and conflating them would lose the repair.**

- A **stale number** is a *claim*. Repair: give the command, not the figure. Failure cost: someone
  believes a wrong quantity.
- A **stale dispatch** is a *decision*. Repair: **the measurement must be part of the send, not part of
  the drafting.** Failure cost: someone is told to do work already done, or told the wrong priority —
  and unlike a wrong number it is not visible to the recipient, who has no reason to doubt the premise
  of an instruction.

Concrete form, no tooling, and we converged on it from both ends within minutes: **a dispatch carries,
at the top, the command and the timestamp of the state it was built on; the recipient re-runs it before
acting.** I had written that sentence at the bottom of my 08:20 message, where it read as a courtesy —
and the Orchestrator has recorded that it *read it as a courtesy and did not re-measure before
dispatching.* **The position of the sentence was a design choice in my message and it worked exactly as
designed, which is not what I intended.**

Neither error cost anything today, and the reason is worth stating plainly rather than banking:
**Plan was already doing the right thing in both cases. That is luck, not method** — the Orchestrator's
phrase, and I am not going to soften it.

### 6.0b A third form, five minutes later: the address itself went stale

The message carrying §6 failed to send: `connect ENOENT /tmp/cc-socks/1944.sock`. The peer had
restarted; the socket path was a reference to a process that no longer existed, and the failure surfaced
at the **sender**, not the recipient.

Yesterday the Orchestrator repaired *addressing by presumed session name* by switching to sockets. A
socket path is also a reference that goes stale — on restart, silently. So today produced the same
defect at three altitudes within one hour:

| altitude | the stale thing | who discovers it | repair |
|---|---|---|---|
| a **number** | a rate over live branches | anyone who re-runs | give the command, not the figure |
| a **dispatch** | the state a decision was built on | usually nobody — the recipient complies | measure at the send, not at the drafting |
| an **address** | the socket of a restarted peer | the sender, immediately | resolve the address at send time |

The address form is the *least* harmful precisely because it fails loudly at the sender. The dispatch
form is the most harmful for the mirror-image reason. **The general law is the same and it is worth
stating once: every reference has a lifetime, and the only safe moment to resolve one is the moment it
is used.** `PATTERN_ALREADY_SOLVED_GATE` reaches routing too.

**And the sharpest form of it is the Orchestrator's, about itself.** Yesterday it repaired
*addressing-by-presumed-name* — a routing defect that cost hours — by switching to socket paths. Today
the socket path went stale on its own restart:

> *"I replaced a stale reference with another stale reference and called it a repair."*

That is the deepest version of today's law and it generalises past routing: **a repair that swaps one
instance of a class for another instance of the same class is not a repair, and it is hardest to see
precisely because the first instance really was fixed.** The test is not *did the reported symptom
stop* but *is the new thing a member of the same class*. By that test, addressing by socket fails and
`ListAgents`-at-send-time passes, for the same reason `give the command, not the figure` passes: it
resolves at use.

*Verified rather than accepted:* `/tmp/cc-socks/` holds `1883`, `1978`, `2024`, `39715` (this session —
`ppid` confirms it) and `92030`, created 09:03. `1944.sock` is gone. Three of the five have mtimes of
2026-08-10 10:51 and have never restarted.

**One thing I still cannot do from artifacts.** `ListAgents` and the socket directory are two registries
of the same population and they disagree on membership: a session listed yesterday is absent from
`ListAgents` today while its socket persists. **I cannot map actor names to sockets from the artifacts
alone** — the same `actor` attribution gap recorded on 2026-08-10, one layer down, in routing rather
than in the ledger. Every mapping I hold was told to me.

### 6.1 Item 5 stays open, as a structural claim and not a closed incident

At the Orchestrator's request, and it is right to ask. This morning's validator divergence closed
because **A merged `main` at 08:28**, not because anything prevents a branch from being judged at merge
by a rule its own tool does not know.

> *"Closed because A merged, not because anything prevents it"* is the difference between a fire put out
> and a fire that cannot start. The first one comes back.

So it is not withdrawn with items 1 and 4. It is **reclassified**: from an incident with a count to a
standing structural claim with no detector. It closes when `validation_context` exists — a verdict
naming the validator commit that issued it — and not before. Until then the mechanism is intact and its
absence is invisible: every gate was green all night while ten manifests across three branches were in
that state.

---

## 7 — Both triggers re-measured at 11:4x, and neither has fired

Commands, not memory. `main` moved `7101ac4` (08:54) → **`d06a4a1` (11:38)**, nineteen commits today.

**Trigger 2 — `lettore` lands and `PMID42422765` closes: NO.**
`git show main:…/deepdive_manifests/PMID42422765.json` still reads `receipt:
FTR-20260810-42422765-04`. `-05` exists in the ledger on `main` and is the complete read: `-04` carries
`methods: not_read` and `supplementary: unavailable` — the false premise A retracted — while `-05` has
`methods: read`. **The manifest is still anchored to the receipt with the retracted premise in it.**
The item is open, unchanged, and its closure is still `lettore` landing with `--repoint-manifests`.

Ledger population: 93 records on `main`; 9 mention `42422765` anywhere in the record, of which **6 are
about the study** (`-01` legacy through `-05`) and **3 are chain-neighbours of other studies** whose
`prior_receipt` names one of these. I matched on the whole JSON blob, so a third of my own hits were the
wide-probe error I have written up twice. Exhibiting the complement is what showed it; the count alone
would have read as a clean 9.

**Trigger 5 — `validation_context` exists: NO.** Zero occurrences in `main:framework/scripts/`. So item 5
is rewritten this morning as promised, unchanged in substance, and it will be rewritten tomorrow.

### 7.1 A line count over a hash chain reports length, not lineage

The Orchestrator measured the five ledgers with `wc -l` and read `lettore-b` at 88 as *"five receipts
behind"*. The number is right and the operation it implies is wrong. Byte-exact longest common prefix
against `main`'s 93, at the commit tips:

| branch | records | shared prefix | its own | on `main` only | merge shape |
|---|---|---|---|---|---|
| `lettore` | 95 | 84 | 11 | 9 | rechain of 11 |
| `lettore-b` | 88 | **87** | **1** | **6** | rechain of 1 |
| `evidence-index` | 93 | 93 | 0 | 0 | prefix, nothing to do |
| `mirror` | 63 | 63 | 0 | 30 | prefix, stale by age |

`lettore-b` is not five behind: it is **six behind and one ahead**, and its record 88 is not `main`'s
record 88. "Behind" describes appending; this needs the rechain. **`append_only_prefix` is the predicate
this repo already owns** — it is what the ledger's own guard is built on — and it was not the one reached
for. Third instance in my log of a guard that exists at one site and is not carried to the second.

**And the thing the prefix probe surfaces that no count could.** Six `event_id`s live on both `lettore`
and `main`. That is the exact shape of the identifier-minting collision class I logged yesterday — same
name, two branches, no shared allocator. It is not one: comparing payloads, all six differ **only** in
`ledger_prev_hash`, i.e. the same event rechained into a different position. `lettore-b`: 87 shared ids,
**87 byte-identical, 0 divergent**.

> **`event_id` equality across branches has two causes with opposite meanings — the same event rechained,
> and two different events minted with one name — and only the payload separates them.** The chain already
> decides it correctly; a probe that compares identifiers cannot.

*OBSERVATION.* The trade-off of the prefix probe over `wc -l`: it reads both full files instead of
counting lines, and it is per-pair rather than per-branch. At five branches and ~100 records that is free;
at the scale this repo designs for it is a diff, not a length, and should be bounded by the divergence
point rather than by the file.

### 7.2 Two sibling writers, and each holds the property the other lacks

`d06a4a1` (11:38) fixed `sync_epochs.append_event`: it appended the event, then failed to anchor it,
leaving precisely the state its own reader calls `SYNC_EPOCH_LEDGER_UNTRUSTED` / `BLOCK_SYSTEM`. The
commit names the class honestly and counts it — *"the fifth instance in two days of a check that verified
the shape of an outcome rather than the outcome."*

I went looking for the class elsewhere, in the one place it should not be: **the sibling that module was
modelled on.** `fulltext_receipts.append_receipt` has had the property all along — the anchor is validated
**before** the sequence is extended (`main:framework/scripts/fulltext_receipts.py:737`), and its two
refusal tests assert the ledger afterwards (`load_ledger(...) == []`, `len(...) == 2`), not merely that
something was thrown. Its docstring even *explains why*: *"Checking the anchor before extending is what
[stops the tamper being laundered into verified history]"*.

So the knowledge was implemented, tested **and written down at the site** — and the newer writer, shipped
one commit before it broke, did not inherit any of the three.

The asymmetry runs both ways, which is what makes it a practice rather than an anecdote. Test-class
inventories:

- `test_sync_epochs.py` has **`TheWriterTouchesNothingElse`** — it pins on the *source* that the module
  can never move a checkout or touch a scientific file. `test_fulltext_receipts.py` has no equivalent, and
  the idiom appears in exactly **2 of 861** test functions repo-wide.
- `test_fulltext_receipts.py` has the anchor-before-append ordering and the state-after-refusal
  assertions. `test_sync_epochs.py` acquired them today, by breaking.

*CANDIDATE PRACTICE.* **When a module is modelled on a sibling, diff the two test-class inventories before
reviewing the new code.** Two `grep '^class '` and a reading of the names. Falsifiable success criterion:
run it on the next pair of siblings that ship, before the second one's first defect; it succeeds if it
names a property the new module lacks *and* one the old module lacks, as it did here, and fails if the
inventories are already congruent — in which case the practice costs ten seconds and is dropped.
**Trade-off:** class names are prose, so the diff is a reading, not a check; it finds absences that were
*named* and is blind to properties nobody bothered to name in either file.

### 7.3 On the probe that got me there — withdrawn as a measurement

I built a census: 861 test functions on `main`, 69 containing a raise-assertion, **57** with no state
assertion after it. I opened two. **Both were false positives** — they assert `load_ledger(...)`, a
domain-specific state read my vocabulary did not know. The census is therefore **withdrawn**; it survives
only as the route by which I reached §7.2, which was adjudicated by opening the source.

This is the third mis-patterned probe I have published in two days, and the pattern of the pattern is now
visible: **all three were wide, and all three were wide in the same way — a hand-written vocabulary of
what "counts", applied to a domain whose vocabulary I do not own.** The complement rule caught the first
two by arithmetic. It did not catch this one, because a false positive is *inside* `N`, not in `P − N`.
For an over-matching probe the complement is the wrong exhibit; **the right one is an adjudicated sample
of `N` itself**, which is what two opened files were. One line for the standing rule: *exhibit the
complement to find what you missed, open a sample of the matches to find what you invented.*

### 7.4 Attribution: I do not have to rest on the Orchestrator's word

Its reply corrects me and the correction is right. `git branch --contains <sha>`, the worktree whose
`HEAD` is that branch, the receipt the commit emits and its chain parent, and the reflog order — all of it
addresses **by content**, which is the defence this repository uses everywhere except where I needed it.
I adopt it: from here an actor in these reviews is identified by its branch, verifiable by the reader.
Where attribution genuinely needs a declarant, it is declared `UNRESOLVED` and not resolved by fallback.

---

## 8 — I priced an operation from 20% of its surface, one message after naming that error

The Orchestrator inverted §7.1's operational reading — *"a rechain of one record, i.e. one line of work"* —
and kept the measurement. It was right, and the reason is mine to take rather than to debate. I measured
the **ledger** and priced **the merge**. `git merge-tree --write-tree main lettore-b`, executed, no merge:

> 23 files, 3,260 insertions, **5 conflicting files** — `fulltext_read_receipts.jsonl`,
> `discovery_ledger_current.md`, `full_text_queue_current.md`, `learned_gates_registry.md`,
> `state_manifest_current.md`.

The ledger is **one conflict of five**. I generalised from the surface I happened to be holding, in the
same message in which I criticised a `wc -l` for exactly that. The class is not "wide probe" this time:
it is *a measurement of one surface used to price an operation whose surface is larger* — and the tell is
identical, a number that is correct and an operation that is not the one implied. **A cost is not a
measurement until the thing being priced has been enumerated.**

### 8.1 The validator gap is live on both reading branches, and named

Set-diff of the validator's vocabulary by `ast`, not by counting lines — `errors.append` on its own line
undercounts, most refusals wrap:

| ref | bytes | functions `main` has and it does not | constants |
|---|---|---|---|
| `main` | 89,194 | — | — |
| `lettore` | 76,787 | `font_encoding_verdict`, `font_is_symbolic` | `ENCODING_SENTINELS`, `SUBSET_PREFIX`, `SYMBOLIC_FONT` |
| `lettore-b` | 77,177 | the same two | the same three |

Both reading branches are missing the whole of `9296ad7` (10:30) — rule 5d at the level of the font, the
check for embedded subsets that declare `WinAnsiEncoding` with no `ToUnicode` CMap. Item 5 is not
hypothetical this morning: **it is running, on both branches, on the check that landed today.**

### 8.2 And its consequence today is zero, which is the finding

`9296ad7` names four files `UNTRUSTWORTHY` with no sentinel at all: `18487609`, `23370280`, `24550385`,
`26499798`. Three have manifests. **All three rest on PMC:**

| manifest | `article_text` | `article_binary` | locators |
|---|---|---|---|
| `main` `PMID24550385` | `…_PMC.html` | the PDF | 20 (15 body · 5 figure) |
| `lettore` `PMID18487609` | `…_PMC.html` | the PDF | 23 (15 body · 8 figure) |
| `lettore` `PMID23370280` | `…_PMC.xml` | the PDF | 12 (9 body · 3 figure) |
| — `PMID26499798` | no manifest on any of 17 refs | | |

The font defect is a property of the **PDF text layer**. In all three the PDF is fingerprinted as
`article_binary` and is not the surface any locator is matched against. So the branches lack a guard whose
bite, on precisely the papers it flags, is **nil** — because rule 5d's primary instruction, *prefer
XML/HTML PMC over the PDF, always*, had already been obeyed by both readers without the guard existing.

*OBSERVATION, and it argues for the Orchestrator's call over my own.* The check is a **backstop for a
surface these readers are not using**, which is why its absence on a reading branch is not urgent — a
better reason to hold the merge than the one either of us gave. It has teeth in the complement: where
there is no PMC. `PMID 17803050` — no DOI, no PMCID, all rights reserved — carries the **largest manifest
in the repository, 32 locators**, and exists on 11 of 17 refs.

### 8.3 One claim in `9296ad7` I could not resolve

> *"One of those four backs a complete read at 39/39 locators."*

Searched every ref: **17 refs, 44 distinct manifests, zero with 39 locators.** The four resolve to 23, 12,
20 and none. The maximum anywhere is 32 (`PMID17803050`). Not "false" — **unresolved**: `39/39` may count
receipt-level verifications rather than manifest entries, or name a paper outside the four. It is the
author's number and the author has the pattern that produced it. Flagged, not corrected.

---

## 9 — The two merge numbers disagreed because they price the two directions

The Orchestrator reproduced the five conflicting files exactly and got a different volume: **7 files / 534
insertions** against my **23 / 3,260**. It declined to choose between them or average them, and handed the
disagreement on undecided. That was right, and the resolution is one command — the rule that a number
comes with the pattern that produced it, turned on two numbers instead of one.

`merge-base(main, lettore-b) = 7101ac4` (08:54). At `main d06a4a1`, `lettore-b ccd1e1f`:

| command | files | insertions | question it answers |
|---|---|---|---|
| `git diff --shortstat lettore-b...main` | **23** | **3,260** | what `main` brings *to `lettore-b`* |
| `git diff --shortstat main <merge-tree>` | **7** | **534** | what the merge adds *to `main`* |
| `git diff --shortstat main...lettore-b` | 7 | 522 | `lettore-b`'s own work since the base |
| `git diff --shortstat lettore-b..main` | 25 | 3,239 | two-dot: adds undoing `lettore-b` |

**Neither is wrong. They price the two directions of the same merge.** Mine is the cost B pays to come up
to date; the Orchestrator's is the cost Plan pays to integrate B. The measurement that was missing is not
a number, it is the **direction** — and *"the merge"* had been carrying both the whole time.

That is the class from 2026-08-10 in a new place: **one name carrying two orthogonal facts**, the class
that already ate `surface` (where vs how read), `actor` (person vs branch), `citation_role` (path vs role)
and `event_id` (rechained vs collided). This is its first instance in a *spoken measurement* rather than a
stored field — which is worse, because a field can at least be given a second slot, and a sentence cannot
be validated. Two actors each measured correctly and disagreed for three exchanges.

*CANDIDATE PRACTICE, cheap enough to be a reflex.* **A merge cost is quoted with its direction, or it is
not quoted** — `X→Y`, never "the merge". Falsifiable: the next time two actors quote a merge volume,
either the directions are stated and the numbers reconcile, or the practice did not take. **Trade-off:**
none worth the name; it is four characters. The reason it is a candidate and not a rule is that one
reconciliation is one data point, and I have been wrong about promoting on one before.

### 9.1 What the direction changes, operationally

The Orchestrator's 7/534 is the *integration* side and it is small. My 23/3,260 is the side B would pay,
and it is the one that was actually under discussion. So the reason for holding is neither of the two we
gave: it is the **content** of the five conflicts, three of which are state — `state_manifest_current.md`,
`learned_gates_registry.md`, `full_text_queue_current.md` — which do not resolve inside a low-context
actor's window regardless of how many lines they are. The Orchestrator reached that reading first, from
the same table.

### 9.2 The prefix table re-measured, because `lettore-b` moved

`lettore-b` `52bf5c2` → **`ccd1e1f` (14:39)** mid-exchange. Re-run at the current tips: `lettore` 95 /
prefix 84 / own 11 / main-only 9; `lettore-b` 88 / prefix 87 / own 1 / main-only 6. **Unchanged** — B's
commit did not touch the ledger. Stated because the table would otherwise be a remembered number over a
branch that moved, which is the failure I logged against my own `21.7%` yesterday.

### 9.3 `39/39` resolved by the Orchestrator, and its false zero is the better half

`39/39` are **panels inspected as images** on `PMID24550385`, from the receipt on `main` — unique across
93 records. The manifest has 20 locators; the panels are 39. The commit message compressed *panels* into
*locators*: a citation compression, not an invented number. My census was right and my refusal to say
"false" was right; the hypothesis I left open is the one that held.

The part worth keeping is the Orchestrator's account of its **first attempt**: it queried the ledger on
`pmid`, where the key is `study_id` and `pmid` lives inside it, and got *"the four flagged: no records"* —
**a clean, printed zero across four papers that all have receipts.** Same diagnosis as mine this morning,
a hand-written vocabulary applied to a domain whose schema the prober did not hold. And it sharpens the
second leg I proposed: I said *open a sample of the matches to find what you invented*. **A false zero has
no matches to sample and no complement to exhibit.** What caught it was printing the keys of one record —
so the leg generalises to: *before trusting a query, print one record whole.* Cheaper than either.

*OBSERVATION, not a rule.* Four mis-patterned probes in two days across three actors, all four the same
shape. The instrument is not the problem; **the vocabulary is assumed and never printed.**

### 9.4 There is no author to ask

`9296ad7` is signed `The LEGEND project <legend-project@users.noreply.github.com>` — the shared identity.
So §8.3's *"the author has the pattern that produced it"* named someone `git` cannot resolve. The commit
author carries the project, not the actor, and the **branch is the only discriminant** — the correction the
Orchestrator gave me this morning, arriving a second time in the same day at the place where I had not yet
applied it. Here it did not matter: the pattern was in the ledger, not in a person.

---

## 10 — The role, accounted for the second day, in decisions and not in output

My brief says this role is a cost that must be justified in measurement, and that if three rounds of
reviews change no decision I must be the first to say the role is not needed. Day two. The unit is a
**decision that changed**, not a page written — 8 commits and 512 lines are effort, not evidence.

**Changed by this review:**

| decision | before | after | changed by |
|---|---|---|---|
| how the Orchestrator addresses peers | a remembered socket path | copy the `from` of the incoming message, resolved at use | §6.0b |
| B's branch state | "five receipts behind", an append | six behind and one ahead, a rechain | §7.1 |
| B's next task | measure the merge surface itself | stopped; the measurement already existed | §8 |
| how a merge cost is quoted | "the merge" | `X→Y`, or not quoted | §9 |
| the query-validation rule | exhibit the complement | *print one record whole*, which catches the false zero neither earlier form could | §9.3 |

**Changed against this review — which is the half that makes it a mirror and not a megaphone:**

| my claim | what it was | who corrected it |
|---|---|---|
| "a rechain of one record, i.e. one line of work" | a merge surface priced from the ledger alone: 5 conflicts, 3 of them state files | Orchestrator, §8 |
| "the author has the pattern that produced it" | there is no author; commits carry the shared identity, the branch is the discriminant | Orchestrator, §9.4 |
| a census of 57 shape-only refusal tests | withdrawn; two opened, two false positives | me, §7.3 |

Two of my three published errors today were found by someone else, and the ratio is the honest signal: the
review is worth its cost **because it is contested**, not because it is right. A round where nobody
corrected me would be the one to worry about.

**The negative — what I went looking for today and did not find.** A seventh collision class: none, the
day produced instances of classes already named. A guard that fires on the papers it was built for: the
font check bites nothing on all three manifests of the four files it flags. A manifest with 39 locators:
none across 17 refs, because the number was never locators. And a reason to keep my own contract-freeze
proposal: it fails the Orchestrator's own class test, so it stays demoted.

### 10.1 Open list, one line, unchanged since 09:07

**`PMID42422765.json` on `main` names `FTR-20260810-42422765-04`, the receipt carrying the premise A
retracted, while `-05` is the complete read.** Routed, not assigned, not closed. It closes when a commit
closes it, not when a message says so.

And the standing structural claim, rewritten this morning and due again tomorrow: **`validation_context`
does not exist** — zero occurrences in `main:framework/scripts/` — so a branch can still be issued a green
by a validator older than the rule. Today's instance is measured in §8.1 and its bite is zero in §8.2.
Both facts belong in the same sentence, or the claim becomes an alarm nobody will look at.

---

## 11 — The figure-coverage rule, audited against the criterion I wrote before the data

This is an audit of **an instruction**, not of the actors who followed it. The rule was mine and its
criterion was published in advance:

> *"Zero slots `figures: read` with no locator and no named waiver, and the fig/paper ratio between the
> highest and lowest reader below 3× (it was 6.5×). If it stays above, the rule is wrong and I change it."*

**Population and pattern, first.** The reading run is `analysis_at`, not `event_at` — six receipts analysed
last night at 20:50–22:00 were persisted this morning at 06:22, and an `event_at` filter files them under
today. On `analysis_at`: **08-10 = 37 receipts, 08-11 = 12** (A 10 across 8 papers, Codex 1, B 1, plus B's
22 preflights which emit no receipt). Deduped by `event_id` across 17 refs; actor attributed by branch.

### 11.1 First leg: passed, and it is the smaller half of what it looks like

Receipts with `figures: read` and **zero figure locators and no waiver key: 0 today.** Yesterday there
were **2** — `29808465` (`unavailable`, 0 locators, 0 waiver keys) and `34268881` (`captions_only`, 0, 0).
So the empty slot closed, and it was not empty before.

But the leg is narrower than the words suggested. `figures: read` is a word with no denominator: it says a
figure surface was opened, not how much of it was covered. **The rule enforced the presence of a locator,
not coverage.** It closed the empty slot and is structurally blind to the shallow one.

### 11.2 Second leg: the criterion cannot decide, because `fig/paper` never named its denominator

Under the reader's denominator — figure locators per paper — **today: Codex 5.00, A 4.90, B 3.00, ratio
1.67×**. Under 3×. But yesterday, measured identically: **A 6.26, B 7.67, ratio 1.23×** — *already* under
3×, before the rule existed. A criterion that its own baseline passes discriminates nothing.

I cannot reproduce `6.5×` from this measurement, and I am not going to reverse-engineer a pattern that
produces it. The honest statement is that **the baseline and the follow-up were computed by different
patterns**, which is the defect I have charged four times in two days, in my own success criterion.

Under the paper's denominator — panels covered over panels present — the criterion is **not computable**,
because there is no field. And that is the finding, not the excuse:

> **The denominator exists, is being produced by the readers, and has no home.**

Panel fractions stated in prose today: `39/39` (yesterday, `24550385`), `32/32` (`18487609`), `5/17`
(`16061658`), `0/26` (`30356099`), `7/7` (`39952983`). **Three of those five are recorded inside a
different paper's receipt** — `18487609`'s and `16061658`'s appear in `23370280`'s evidence basis,
`30356099`'s appears in `31543760`'s. My first pass attributed each fraction to the receipt containing it
and was wrong on three of five; opening the sentences is what showed it. Of 12 receipts today, **3 contain
a fraction and 9 do not**, and of the papers whose coverage is knowable, most are knowable only by reading
someone else's record.

So the fact my criterion needed was being written down all day, in a place no query can attribute to the
paper it describes. This is `surface: supplement` again — *1 in 315 in the field, 29 in free prose* — and
it is my queued Q6 arriving with a use case instead of a hypothesis.

### 11.3 Verdict on my own rule

**Neither "it worked" nor "it was ceremony". It half-worked, and the half that worked is not the half the
criterion measured.** What it demonstrably did: closed two empty `figures` slots, and — `30356099` is the
proof — produced a *named* gap (`0/26` panels, no image on disk) that a later run closed by acquiring the
surface. A waiver that names a gap specifically enough to be discharged is the rule doing exactly its job.

What it did not do: give the coverage question a denominator, which is what "fig/paper" was reaching for.

**So I change the criterion, not the rule.** Replacement, and it is falsifiable in one query rather than
by comparing two ratios:

> *Every receipt whose `figures` is not `not_present` states `panels_covered / panels_present` **in its own
> record**. Success: the fraction is derivable per paper without reading another paper's receipt. It fails
> if, at the next ten receipts, any paper's coverage is knowable only from a neighbour.*

**Trade-off, stated:** it asks readers to count panels present, which on a dense supplementary figure is
itself a judgement, and a wrong denominator is worse than none because it looks precise. Mitigation: the
count is declared with the surface it was counted on, and `unknown` is a permitted value. *CANDIDATE
PRACTICE* — one day of data, and the last time I promoted on one day I had to demote it.

### 11.4 Did the preflight change the outcome, or the order of the gestures?

Findings per paper (`evidence_basis` entries, a structural field): **A 9.85 → 14.80**, +50%. B's single
receipt today is 15.00 against 9.33 yesterday across three.

The load-bearing measure does **not** support the same reading, and the reason matters. 🔴 per paper goes
A 2.06 → 6.90, but 🔴 is a **convention**, and its adoption moved on the same day: A used it in **11 of 34**
receipts yesterday and **10 of 10** today. That number cannot separate *more load-bearing findings* from
*more findings marked as load-bearing*. I am not going to present it as yield.

Codex is **n = 1 receipt in the entire ledger** and has never used 🔴. Its 11.00 findings/paper against A's
14.80 is not a comparison — the operator's question presumes Codex's preflight predates the rule, which is
true, but the ledger holds one Codex reading and cannot support a per-paper yield claim in either
direction. **UNRESOLVED, and it is not resolvable from artifacts** — it needs Codex receipts, not analysis.

*OBSERVATION with its confounder named:* findings per paper rose 50% for the actor with enough receipts to
measure, on a day when the papers also changed (older Aqeilan corpus) and the receipt convention changed.
The rise is real; its attribution to the preflight is not established.

### 11.5 Did the named waiver hold under pressure? The repository cannot say

I checked every manifest touched today for when its waiver keys entered: **all ten carry their waivers in
the first committed version**, including Codex's `supplement_locator_waiver` on `21075834`.

That does not mean all ten were spontaneous. It means the conversation happens **before** the first commit,
so a waiver written after being asked and a waiver written unprompted are **byte-identical in the
repository**. The operator's distinction is real and the artifact does not carry it.

> **A named waiver records its reason and not its origin** — and a rule satisfiable by prompting is
> satisfied by prompting. My rule's second leg is unauditable by exactly the standard I apply to others.

Waivers are also not a schema field: they are ad-hoc keys whose names the author invents —
`🔴 named_waiver_figure_2_privacy`, `supplement_locator_waiver`, `abstract_anchoring_waived` — sitting
under `reading_budget`, beside a real boolean `verbatim_locators.waived`. An emoji in a JSON key is the
same hazard already logged for wikilinks. **A named waiver cannot be counted, so it cannot be a gate
condition.** That is a defect in my rule, not in the readers.

### 11.6 The class from merit, not method: does the unit hide the effect elsewhere?

Three instances in one day on three independent papers — incoherent premature-death thresholds in Piard,
`p = 0.0626` presented as *enhanced adhesion* in Kosla, betas in hours rather than minutes in Kim. The
operator asks whether it is a property of the literature or of the three papers.

**Not separable from reader attention with today's data**, and the reason is the standing question: *did
you find it, or did you go looking?* All three were found by one actor on one day, and the third receipt
names the unit conversion as the finding — which is what looking for something produces. The falsifiable
test is cheap and needs no new tooling: **it is a property of the literature if it appears in readings by a
different actor who was not told to look.** B's next receipts and Codex's are the sample. Until then it is
a hypothesis with three supporting instances and no negative control.

---

## 12 — §11 corrected forward: four defects in my measurement, and every per-actor number withdrawn

§11 stays on the record; this section supersedes it. The verdict it reached — *the criterion cannot
decide* — survives. **Most of the numbers it reached it with do not.** Four defects, reported by review
and verified here rather than accepted.

### 12.1 The one that voids the most: branch holder is not the reading's author

My script assigned `actor = "lettore" if "lettore" in own else …`. Measured:

> **106 of 112 receipts live on more than one branch. Six live on one.**

So for 95% of the ledger, my attribution reported **my own precedence order**, not an actor. `lettore` was
first in that order, so `lettore` is what almost everything became. `PMID 34831305` — Codex's reading —
exists on ten refs including `codex/pmid-34831305`, and my script calls all four of its receipts `lettore`.

The claim *"Codex has one receipt in the entire ledger"* is **false, and I published it.** It counted the
one Codex receipt still isolated on a `codex/*` branch; every propagated one had been relabelled A. A
receipt changes author when it is merged — which is the `actor` gap I documented on 2026-08-10, applied by
me, against my own warning, in an audit whose subject was measurement discipline.

**Withdrawn: every per-actor statistic in §11.** fig/paper by actor, findings/paper by actor, the 1.67×
ratio, the 🔴 rates, the Codex comparison. Not corrected — withdrawn. There is **no actor field** in the
schema (19 keys, none of them an actor), so they cannot be recomputed.

*Is it reconstructible from stable provenance?* Partly, and it decays. Taking the commit that first
introduced an `event_id` and intersecting the branches containing it, minus the integrator: `21075834` →
`codex/…` ✅, `18487609` → `lettore` ✅, `23435430` → `lettore-b` ✅ — but `34831305`, older and merged
everywhere, resolves to ten refs and names nobody. **Derived provenance works at write time and decays as
merges accumulate**, which is an argument for a field recorded prospectively, with `unknown_legacy` for
everything already written — never a retrospective backfill, which would be reconstruction presented as
record.

### 12.2 Temporal leakage: a current manifest read against a historical receipt

`manifest(pmid, …)` loads the latest available version and applies it to every receipt for that PMID. The
tell was in my own table and I printed it without seeing it: `16061658`, `retrieved_not_read`, showing
**12 figure locators** — locators added afterwards, by a later reading of the same paper.

So every locator count in §11 is *the manifest as it is now*, not as it was when the receipt was written.
That contaminates **both legs**, including the one I reported as passed: a receipt with zero figure
locators at the time can show many now, so "zero empty slots today" may be a false negative. To evaluate a
receipt you need the manifest at the commit that produced it, or a digest the receipt declares. Neither
exists. **UNRESOLVED.**

### 12.3 The denominator called "paper" is a receipt

`a[0] += 1` per event. Yesterday that counts `34831305` four times and `42422765` five; today `16061658`
twice and `23370280` twice. Corrections, partials and completes of one episode became distinct papers.
**`37 yesterday / 12 today` describes raw events, not papers read.** Comparing days requires grouping by
reading episode along the `prior_receipt` chain and taking the last pertinent state.

### 12.4 `len(evidence_basis)` does not measure findings

It holds coverage notes, retrieval, licence, validator provenance, process errors and scientific
observations in one list. The `9.85 → 14.80` rise measures **receipt style and granularity** at least as
much as yield. It cannot answer whether the preflight raised scientific yield, and I presented it as if it
narrowed the question. It does not.

### 12.5 The waiver classifier mixes objects

`"waiv" in key` matches `abstract_anchoring_waived`, which is not a figure waiver at all. So *"almost every
manifest carries a waiver key"* does not mean they carry a **figure** waiver — and the first leg,
*"0 empty slots today against 2 yesterday"*, is **not demonstrated with that predicate**. Combined with
§12.2 it is not demonstrable with the data as stored.

### 12.6 What survives, and it is not nothing

- **Spontaneous vs requested is not reconstructible from the repository.** All ten manifests carry their
  waivers in the first committed version, including the one added on request; the conversation precedes the
  commit. This one is *proved*, and it is a negative — the artifact does not carry the distinction.
- **The panel fractions live in the neighbour's receipt.** `18487609`'s and `16061658`'s sit inside
  `23370280`'s evidence basis, `30356099`'s inside `31543760`'s. Independent of every defect above, because
  it is a reading of sentences, not a count.
- **`analysis_at` is the reading day and `event_at` the persistence day** — but it must be applied *after*
  chain deduplication, and it is only as trustworthy as a declared timestamp.
- **The 6.5× baseline is not reproducible under the same predicate**, and I still decline to reverse-engineer
  one that produces it.

### 12.7 The verdict, published as it stands

| question | verdict |
|---|---|
| effectiveness of the figure rule | **INCONCLUSIVE** |
| the 6.5× baseline | **NOT REPRODUCIBLE UNDER THE SAME PREDICATE** |
| panel coverage | **NOT MEASURABLE FROM THE CURRENT SCHEMA** |
| effect of the preflight on yield | **UNRESOLVED** |
| waiver origin, spontaneous or requested | **UNRESOLVED RETROSPECTIVELY** |
| "the unit hides the effect" | hypothesis: three instances, one actor, **no independent control** |

### 12.8 The proposal goes to Plan, and it is not a free-text fraction

My §11 replacement criterion — *state the fraction in your own receipt* — repeats the defect it was
answering: another number in prose, in a second place, with no counting rule. `0/26` without a counting
rule is **apparent precision**.

One authoritative home, in the manifest beside the figure inventory, the receipt summarising or
referencing it and never duplicating it:

    panels_present · panels_inspected · denominator_basis · denominator_status

`denominator_basis` is what the count was taken on (published figure, PMC rendition, supplementary sheet —
they disagree) and `denominator_status` carries `unknown` honestly rather than forcing a number.
**Prospective only**, `unknown_legacy` for everything already written.

I do not implement it: not the schema, not the validator, not my perimeter. It goes to Plan as a separate
proposal. I have no verified address for Plan — session names do not identify actors, and I am not going to
guess one — so it routes through the Orchestrator, which is the standing rule.

### 12.9 What this says about me, since that is the point of the role

Five of my probes in two days were mis-patterned, and this one was worse than the previous four: it was
**an audit of measurement discipline whose own measurement failed on the exact class I had documented**.
The pattern of the pattern held again — a hand-written vocabulary applied to a domain whose schema I did
not hold. What caught it was not my complement rule and not my print-one-record rule: **it was a reader who
read my code.** That is the third leg, and it is the one I cannot perform on myself.

---

## 13 — The 6.5× resolves, and the rule changed denominator between its own two sentences

The Orchestrator names the quantity: the baseline was **panels per paper** — Codex 2.9 · A 7.0 · B 19.0 —
not figure locators per paper. I measured the second and reported the first as irreproducible.

So the item upgrades, and the new verdict is sharper than the old one:

> **NOT REPRODUCIBLE (cause unknown) → NOT CHECKABLE (cause known): one of its two denominators does not
> exist.** With the quantity named I still cannot check it, because *panels per paper* needs a panel count
> the schema does not hold and *per reader* needs an actor field the ledger does not have. Naming the
> quantity did not make it measurable; it made the reason legible.

**And the deeper thing is in the rule's own wording.** Its baseline sentence says *panels/paper*; its
criterion sentence says *fig/paper*. Same rule, two sentences, and the term silently changes denominator
between them — from the paper's (how many panels exist) to the reader's (how many locators were written).
I read the criterion sentence, measured what it said, and got a number that could not meet its own
baseline.

That is the **sixth instance** of one name carrying two orthogonal facts — after `surface`, `actor`,
`citation_role`, `event_id`, and "the merge" — and it is the first one found *inside a single instruction*
rather than between two actors or two records. The five earlier ones were all recoverable by opening
something: a field, a payload, a command. This one is recoverable only by noticing that two sentences of
the same paragraph mean different things by the same word. **No validator reaches inside prose, and prose
is where rules live.**

*The consequence for how instructions get written here, stated as OBSERVATION not RULE:* a falsifiable
criterion has to name the field it will be measured on, not the quantity in words. *"fig/paper below 3×"*
survived being read by its author and by me; *"`panels_present` per `study_id`, per actor field"* would
have failed at the moment of writing, because two of those three do not exist. **The criterion that cannot
name its fields is the criterion that cannot be run.**

### 13.1 The three-legged rule, settled, with the property that matters

    exhibit the complement          → finds what you missed
    print one whole record          → finds what you invented
    have someone read your code     → finds what your predicate CANNOT see

The third has a property the first two do not: **it is not self-executable.** The first two work because
the defect is in the data and the data can be printed. The third is needed when the defect is *in the
predicate*, and a predicate does not see its own blindness. That is the same reason a blind review found
eight defects two informed reviewers had passed over twice.

### 13.2 Survivors kept separate from withdrawn, as asked

**Withdrawn** (§12.1–12.5): every per-actor statistic, both legs of the figure criterion, findings/paper as
a yield measure, the waiver census.
**Standing** (§12.6): waivers spontaneous and requested are byte-identical in the repository — a *proved
negative*; and the panel fractions sit in the neighbouring paper's receipt — a reading of sentences, not a
count, and untouched by every defect above.

### 13.3 The proposal is deferred, and deferred is not shelved

Routed to Plan as a **registered open item**, not as work for this window, on the Orchestrator's three
stated grounds: it is schema, therefore contract, therefore the operator's; it costs the merge window
where thirteen readings are waiting on branches; and nothing breaks while nothing changes. Same treatment
as Plan's own third-surface proposal two hours earlier, which is the consistency test I would have applied
to it.

It stays in my open list until a commit closes it — the same standard as `PMID42422765`.

---

## 14 — Three rectifications accepted, and a fourth that neither of us saw: the reduction already exists

### 14.1 Not "byte-identical" — and the corrected form is the better negative

I wrote that spontaneous and requested waivers are *byte-identical in the repository*. They are not; the
manifests are different files. What I actually demonstrated is narrower and more useful:

> **Spontaneous versus requested provenance is not distinguishable in the persisted representation. The
> repository keeps the waiver's *reason* and not its *origin*.**

It names **which information is missing** instead of claiming two objects coincide. My phrasing overstated
in the direction that sounds stronger, which is the direction to distrust.

### 14.2 "The ledger's unit is the receipt, not the paper" was too strong — and the truth is worse for both of us

`study_id` carries pmid and doi, so papers group. What I found in my script was a defect in **my script**,
and it was generalised — by the Orchestrator, from my wording — into a missing capability of the system.

Then I went to check whether the reduction rule was merely undefined. **It is not undefined. It exists, it
is committed, and neither of us looked.**

`framework/scripts/reading_state.py` implements `study_key`, `summarise`, a `COVERAGE_RANK` lattice, and
explicit handling of `prior_receipt` siblings — parallel reading distinguished from correction, union
rather than last-writer. It writes `disease-models/wwox/registries/reading_state.md`, committed on `main`,
whose header reads:

> **58 paper(s) with at least one receipt · 93 receipt(s) · 1 paper(s) read in parallel.**

That is the exact denominator I hand-rolled a probe to approximate and got wrong. Worse: the tool's
docstring describes **the precise failure mode both of us produced** — *"a reader following the ledger sees
the LAST receipt and takes it for the state of that paper"* — and it also carries the per-checkout scoping
that answers my cross-branch counting: *"a count over unmerged state is a count of work that is not in the
model."*

**Fourth instance of `PATTERN_ALREADY_SOLVED_GATE` in my log, and the sharpest.** The first three were a
guard applied at one site and not the second. This one is a guard applied at the right site, committed,
generating a page in the registries — and two actors spent an afternoon discussing the problem it solves.
CLAUDE.md says it in one line: *before building a guard, look for it — it is probably already here.* I
grepped `prefix`, `anchor`, `tracked`, `scope` yesterday and did not grep `study` today.

**So §12.3 corrects forward again:** the paper denominator is neither absent nor ambiguous. It is
**defined, implemented and published**, and my measurement did not use it.

### 14.3 "The third leg is not self-executable" was true only at the moment of discovery

The corrected form:

> **An independent review discovers the blind class; a regression test makes it self-executable
> afterwards.** Leaving it non-executable wastes the finding.

Which is this repository's own standing rule — *a contract enforced only by a reporting tool is
documentation* — pointed at a review instead of a script. The four fixtures the day's work determined,
registered here beside the proposal and **not implemented**, since schema and validator are not my
perimeter:

1. a receipt propagated onto several branches does not change author;
2. a historical receipt does not receive a future manifest;
3. `partial` and `complete` of one episode do not become two papers;
4. `abstract_anchoring_waived` does not count as a figure waiver.

Each is a defect that actually occurred today, in my own measurement, which is the only provenance that
makes a fixture worth writing.

### 14.4 The proposal, with the operator's two additions

Confirmed in the manifest, non-urgent, held by Plan as a registered open item, plus two things neither the
Orchestrator's version nor mine had:

- **counts per artifact and per surface, not one global scalar** — which is `denominator_basis` taken
  seriously: published figure, PMC rendition and supplementary sheet disagree, so one number per paper
  hides the disagreement it exists to expose;
- **an `actor_id` distinct from the runtime endpoint, never reconstructed retrospectively** — the field
  §12.1 found missing, with the separation that keeps it from becoming a socket path, and the prohibition
  that keeps a backfill from producing a record that looks like data.

### 14.5 Front closed

No schema, no validator, no implementation. Plan holds the proposal and the four fixtures. §12 stands as
published, with §13 and §14 correcting it forward rather than rewriting it. Open list, unchanged:
**`PMID42422765.json` names `-04`**, plus the deferred proposal, each closing on a commit and not on a
message.

---

## 15 — The gate has two victims, and the operator is owed the in-scope number

The Orchestrator ran the tool and found it cuts against its own figure too: *"13 unique PMIDs reaching
`complete` today"* is a count over **20 unmerged refs**, which is precisely what the page's own header
declares is not in the model — *"a count over unmerged state is a count of work that is not in the model."*
Its predicate had been declared, as the operator asked. **What had not been declared was the scope**, and
scope is what the tool publishes in bold.

So `PATTERN_ALREADY_SOLVED_GATE` claimed **two** measurements this afternoon, not one: my per-actor
statistics and a daily count that had already gone to the operator. Both hand-rolled, against a committed
tool that answered both questions.

**The in-scope numbers, from the published page on `main`, not from a probe of mine:**

| state (deepest receipt, union per paper) | papers |
|---|---|
| `complete_fulltext_read` | **33** |
| `partial_fulltext_read` | 22 |
| `abstract_only` | 2 |
| `queried_not_full_read` | 1 |
| **total with at least one receipt** | **58**, over 93 receipts |

**What I will not supply is a merged "today" figure.** The page carries no date dimension, and deriving one
means grouping receipts by `study_id` under the same union-not-last-writer rule — i.e. hand-rolling the
reduction again, four hours after establishing that hand-rolling it is the defect. Either the tool gains a
date filter, or the number does not exist. **`UNRESOLVED`, and deliberately so.**

*A note on how close this section came to being wrong.* My first count of that table grepped every cell and
returned "135 legacy, 33 complete" — the 135 are `unknown_legacy` **coverage cells**, not papers. I printed
the header and two rows before publishing, and the second leg caught it. It is the first time today the
discipline worked **before** publication rather than after, which is the only place it saves anything.

### 15.1 The operative form of the gate, named by its own failure

> *"Yesterday I grepped `prefix`, `anchor`, `tracked`, `scope`. Today I did not grep `study`."*

**The vocabulary to search changes with the problem, and it is not the vocabulary of the last time.** That
is why the gate keeps catching people who have already internalised it: the rule is remembered, the word
list is reused, and the word list is the part that had to change.

---

## 16 — A caveat does not travel with the number it is attached to

I handed the Orchestrator two figures for the incoming reader — `29724996` at 15 figure locators of 25,
`25331887` at 12 of 29 — labelled *orientation, not measurement*, with the temporal-leakage caveat of
§12.2 attached. It declined to forward them, and the reason is better than my label:

> **A number that travels with a caveat arrives as a number.**

And the decisive half is the second one: **they change nothing the reader must do.** The dispatch already
tells the reader to count the denominator by reading captions, because `<fig>` on HTML returns zero. An
unreliable number that does not change the action adds exactly one thing — the risk of being used as a
denominator, which is what `NOT MEASURABLE FROM THE CURRENT SCHEMA` was supposed to prevent.

So the test is not *is the caveat honest* but **does the number change what the recipient does**. If it
does not, the caveat is not a safeguard, it is a permission slip I wrote myself.

This is the same class as §12.1, arriving from the opposite side. There I withdrew numbers because they
were wrong. Here the numbers are what they are and should still not be sent, because *correct but
unactionable* and *incorrect* fail the same way once the label falls off in transit — and labels fall off
in transit. Yesterday's *"a stale number is a claim, a stale dispatch is a decision"* was about the
recipient's ability to doubt; this is about the recipient's ability to **notice there is something to
doubt**, which a forwarded figure strips.

*RULE, and I am promoting this one past candidate because it needs no data to justify — it is a
restriction on my own output, and its trade-off is that I say less.* **A measurement leaves this role only
if it changes a decision. Everything else stays in the review, where it is read with its section.**

### 16.1 Closing state

Front closed. Identity resolved at use, never by table — and the operator has narrowed even the
corroborating evidence: **start-time correlation is corroborative only, never identity resolution and never
routing evidence.** Which leaves copying the `from` as the only form that holds.

Open list, one line each, both closing on a commit:

- **`PMID42422765.json` names `-04`**, the receipt carrying the retracted premise, while `-05` is the
  complete read;
- the deferred `panels_present · panels_inspected · denominator_basis · denominator_status` proposal,
  held by Plan with the four regression fixtures of §14.3.

And the standing structural claim, due to be rewritten tomorrow morning if the mechanism still does not
exist: **`validation_context` — zero occurrences in `main:framework/scripts/`.**

---

## 17 — The guard is a property of the session, and I read a number off a list

### 17.1 Off by two, and the cause is not arithmetic

I told Plan the shared checkout was *"4 commits behind `main`"*. Measured:
`git rev-list --count 9296ad7..main` = **2** — `ead319a` and `d06a4a1`.

The cause is worth more than the correction. I did not compute it; **I counted lines in a `git log` listing
I had printed hours earlier and read the position off it.** It is the same shape as everything else logged
today — a number produced by a gesture rather than by a predicate — and the smallest instance yet, which is
why it slipped past a day spent on exactly this. Plan's phrasing is the right reason to fix it: the
argument survives the correction, but **the number circulates.**

### 17.2 The guard is a property of the session — and that turns a distinction into a finding

`CROSS_WORKTREE_GIT_GUARD` measured on four sessions today:

| session | born in | `git -C <shared checkout>` |
|---|---|---|
| Plan (`evidence-index`) | its worktree | **refused** |
| Mirror | the shared checkout | `exit=0`, **not refused** |
| B | the shared checkout | not refused |
| A | the shared checkout | (reported the same by Plan) |

**Three actors have been working with no boundary at all** — not merely in the wrong directory. The guard
is not an harness property in the abstract; it is a property of *where the session was born*, and the
worktree existing has nothing to do with it. `worktree exists ≠ the session was born there ≠ the harness
enforces its edge` is Plan's formulation and my own session is the third confirming instance.

This is why *"5 of 6"* had to be reported as `WORKTREE_LOCATION_FAIL` and not as a pass with a footnote: the
first criterion is the one that failed, and the sixth is a *separate* fact about enforcement. Two outcomes,
never one word.

### 17.3 What the reopen costs Mirror: nothing, and it is measured rather than assumed

I asked before rather than after, because reading every branch *is* the role. Plan measured it from a
session where the guard **does** bite: `git show lettore:…` → 105 lines, `git show mirror:CLAUDE.md` → correct,
`for-each-ref` → 25 refs. I measured the same from inside `worktrees/mirror`: `main`'s reading-state page,
`lettore`'s 105 receipts, 18 refs.

**The guard refuses the shared checkout's *path*, not the *refs*.** The object database is common, so
cross-branch reading survives isolation intact. The one thing absent from my worktree is `files/` —
gitignored, resident only in the shared checkout. I do not depend on it; **A and B do**, and that is
CLAUDE.md rule 6 rather than an inconvenience.

### 17.4 The detachment is declared state, not drift

I described the shared checkout as *behind* `main`, which implies drift. It is not.
`SYNC-20260811-001` in `sync_epochs.jsonl` records `action: NO_MOVE`, `branch_or_detached: DETACHED`,
`required_action_by_readers: "None. Do not chase main; finish the artifact in hand from its declared base."`
Reattachment is scheduled for the next `BATCH_COMMIT` — because `staging/` exists only in the shared
checkout and a commit on a detached HEAD is orphaned — and will be **announced by a SYNC_EPOCH before the
move, not after**.

One detail in that record deserves naming, because it is the practice I have been arguing for all day,
already in production: its third observation is attributed — *"NOT measured by Plan: the harness refuses
even a read-only `git -C` into another checkout from this worktree, so this value is the orchestrator's
observation and is recorded as theirs."* **A measurement that names who could not make it is worth more
than one that quietly borrows.** That is `validation_context` in spirit, written by hand, in a ledger, by
the actor whose isolation forced the question.

---

## 18 — The prediction, written before the experiment that tests it

### 18.1 The reopen is a falsifier, and today is the last moment it can be recorded honestly

Four observations of `CROSS_WORKTREE_GIT_GUARD`, zero exceptions, **one value per side**: one session born
in a worktree, refused; three born in the shared checkout, not refused. That is a correlation, and Plan
keeps the delimited form — *this Bash path, this harness, now*.

The operator's reopen tests it. So, written down **before** it runs:

> **PREDICTION.** After `cd /Users/massimo/Desktop/legend-public/.claude/worktrees/mirror && claude`, the
> six-command proof returns `WORKTREE_LOCATION_PASS` **and** `CROSS_WORKTREE_GIT_GUARD_OBSERVED` — `pwd`
> and `--show-toplevel` give the worktree, `--show-current` gives `mirror`, and
> `git -C /Users/massimo/Desktop/legend-public status --short` is **refused**.
>
> **FALSIFIER.** If location passes and the sixth command still returns `exit=0`, the explanation *"born
> there"* is dead, the mechanism is something else, and four observations with no exceptions will have
> taught us nothing. That outcome must be reported, not explained away.

Recorded here because a prediction written after the result is not a prediction, and because I will be a
different session when it runs — one with none of this conversation. The file is the only thing that
carries it across.

### 18.2 The pair, and the property that makes both invisible

Plan puts its own error beside mine, and they are two sides of one axis:

| | what was read | what was owed |
|---|---|---|
| Mirror | a **position** in a printed list | a count from `rev-list` |
| Plan | a **number** without its noun — *"39/39 locators"* | 39/39 *panels*, hours after writing the ledger line forbidding the gesture |

Both results were **well-formed**, and that is the whole point: a well-formed wrong answer is the one class
no shape check can see. It is the same statement CLAUDE.md makes about a corrupted text layer — *"a file
can be well formed and declare the false"* — arriving at the level of a measurement instead of a font.

And Plan names the factor I had not: **mine was too small to trip any attention threshold.** The day's other
instances were all large enough to invite a check. A two-versus-four on a commit count is not. **The
defence that scales with size is the one that fails on the small case**, which is where a wrong number
survives longest, because nobody re-derives a number that could not have mattered.

### 18.3 The practice had a name before the mechanism, and that is the right order

`SYNC-20260811-001` attributes a value its author could not measure to the actor who could, because — Plan's
reason — *in a ledger there is no way to tell a borrowed measurement from a taken one*. I read it as
`validation_context` written by hand. Plan's reply is the part worth keeping: **the mechanism without the
practice becomes a field somebody fills in.**

That is the standing argument against my own queued upgrades, made better than I made it. Every one of them
— `provenance`/`readout`, the actor field, `panels_*` — is a field, and a field inherits whatever
seriousness already exists around it. `panels_*` is the proof in the negative: **the field is not missing.
Two forms of it already exist — 12 manifests with `panel_coverage` in free text, 3 with a
`figure_coverage` dict, 0 with both — and no code reads either.** Adding a third form to a codebase that
reads neither of the first two would be the field, without the practice, exactly as Plan describes it.

---

*Nothing here is a reading. The science is A's and B's; this is a note about the machinery that receives
it — and today the machinery was faster than the note.*
