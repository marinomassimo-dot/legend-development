# Orchestration review — 2026-08-11 (Mirror, round 1)

Opened because a measurement found something, not because the date changed. Read-only; every number
below is printed with the command that produced it, and every count with its complement.

Yardstick: `main@415bb71`, `framework/scripts/*` as they stand there, artifacts from the shared checkout.

---

## 1. Diagnosis

1. **`main` holds a 25-locator manifest whose receipt exists on no merged branch, and every gate is
   green.** `PMID29724996.json` names `FTR-20260810-29724996-01`; that receipt lives only on
   `lettore-b`, which is unmerged. A merge carried the manifest without the ledger entry that attests it.
2. **Yesterday's collision repair was correct in the ledger and incomplete outside it.** The renumbering
   that resolved `FTR-20260810-42422765-02` re-pointed a back-reference nobody updated: `PMID42422765.json`
   on `main` says `receipt: …-04`, and the receipt that claims it is `…-05`.
3. **The cause of both is one missing direction.** `session_self_eval` verifies *receipt → output* and
   says so in its own verdict — *"every declared output resolves"*. **Nothing verifies output → receipt.**
   5 of 34 manifests on `main` fail the reverse direction while every check passes.

---

## 2. Material observations

### O1 — The reverse direction, measured corpus-wide (OBSERVATION, high confidence)

Partition over **all 34** manifests on `main`: does `manifest.receipt` appear in that receipt's `outputs`?

```
  ok: 29     not ok: 5

  PMID29724996   receipt=FTR-20260810-29724996-01   RECEIPT ID NOT IN THE LEDGER
  PMID42422765   receipt=FTR-20260810-42422765-04   claimed instead by FTR-20260810-42422765-05
  PMID22193544   receipt=FTR-20260726-22193544-02   claimed by nobody
  PMID34214506   receipt=FTR-20260726-34214506-02   claimed by nobody
  PMID35716775   receipt=FTR-20260726-35716775-02   claimed by nobody
```

*Command:* load the ledger, walk every manifest, check membership of the manifest's own path in the named
receipt's `outputs`; complement (the 29) counted and reported.
*Concurrent state of the gates:* `legend_lint` **PASS** · `session_self_eval` **PASS — "every complete
read has landed and every declared output resolves"** · 84 receipts, 38 complete events.

Three sub-classes, and they are not equally severe:

| # | manifest | class | severity |
|---|---|---|---|
| 1 | `PMID29724996` | the named receipt exists on **no merged branch** — `grep` over `main`, `lettore`, `evidence-index`, `codex/*` returns 0; it is on `lettore-b` alone | **the manifest is on `main`, its attestation is not** |
| 2 | `PMID42422765` | off-by-one created by yesterday's renumbering | a real reference pointing one receipt too early, at a superseded partial |
| 3 | three 2026-07-26 manifests | their receipts predate manifests being listed as outputs | legacy, and honest to leave — but currently indistinguishable from 1 and 2 |

### O2 — Sub-class 1 is the mirror of a rule already written (OBSERVATION)

`CLAUDE.md` states it from one side: *a branch carries the manifest; it does not carry the evidence.*
This is the same hazard from the other: **a merge carried the manifest and left the ledger entry
behind.** The rule exists, at one site, in one direction. Same shape as every other finding of
2026-08-10 — the pattern applied unevenly rather than misunderstood.

### O3 — Sub-class 2 is the seventh collision class, paid (OBSERVATION, closes a 2026-08-10 prediction)

The `FTR-20260810-42422765-02` collision I recorded yesterday is **resolved on `main`, and resolved
correctly in the ledger**: A's receipts were renumbered 02→03, 03→04, 04→05, and re-parented so that
A's first continuation chains to `-01` rather than to Codex's supplement reading. That is exactly the
repair the collision required.

What was not carried: `PMID42422765.json` still names `-04`. **Renumbering an identifier repairs the
ledger and silently re-points every reference held outside it** — the *position/identity* class, at a
new site. `lettore` still holds the un-renumbered chain, so whoever merges it must take `main`'s
numbering and fix the manifest in the same commit; `git merge-tree main lettore` → 3 conflicted files,
the ledger among them.

### O4 — The triggers I left open (measurement, no finding)

| trigger | state |
|---|---|
| the contract freeze | **held** — `deepdive_manifest.py` unchanged since 22:37 on 08-10 |
| `provenance` / `readout` shipped | **no** — `grep -cE '"(provenance\|readout)"'` on `main` → 0. §6.1's interval stays **untested** |
| `validation_context` shipped | **no** — 0 |
| `lettore-b` landed | **no** — my 2026-08-10 review still blocks `BATCH_COMMIT` on `38499540`, by design |
| `codex/pmid-34831305` landed | **yes**, and `codex/pmid-42422765-s8` with it |

---

## 3. Proposal

**Check the reverse direction, in the tool that already loads both sides.**
`session_self_eval` reads the ledger and walks the manifests today; it verifies *receipt → output* and
stops. The addition is the membership test in O1's command and nothing else: for every manifest, the
receipt it names must exist **and** must list that manifest among its outputs.

*Falsifiable:* run it now — it must report exactly the five above, no more and no fewer. Run it after the
repair — zero, and it must **stay** zero across the next merge that renumbers anything.
*Trade-off, and it is real:* the three legacy manifests will fail on day one for a reason that is not a
defect. Either they are grandfathered by a ratchet — the `non_increasing` shape `growth_anchors`
already implements, so no new mechanism — or the check ships noisy and is ignored within a week, which
is the failure mode this repository has already paid for.

**Not proposed:** a new script. The data is already in one process; a second tool would be the third
site of a pattern that exists twice.

---

## 4. The negative — what I looked for and did not find

- **A sixth broken back-reference.** 29 of 34 are clean, and the 29 were counted, not assumed.
- **Any gate that already catches this.** `legend_lint` PASS, `session_self_eval` PASS, `growth_anchors`
  measured; `build_evidence_index.py:159` reads `manifest.get("receipt")` but only to index it.
- **Contract movement overnight.** Zero commits to `deepdive_manifest.py` since 22:37. The freeze held
  for the first stretch since it was proposed — recorded as an observation, not a vindication: three
  hours of a quiet night is not evidence about a rule.
- **A second renumbering casualty.** Only `PMID42422765` references the renumbered range from outside
  the ledger; `-02`, `-03` and `-05` are named only by `reading_state.md`, a session evaluation, the
  protocol and the test suite, all of which are on `main` and internally consistent.
- **`analysis_at` collisions (2026-08-10 item 13).** Still open, still nobody's. Not measured today
  because it needs an answer from Codex, whose budget is exhausted.

---

## 5. 09:00 — half of §1 resolved itself in forty-five minutes, and that is the finding about me

Re-run of O1's partition on `main` at 09:00, unchanged command:

```
REVERSE DIRECTION on main — 36 manifests (was 34)
  ok: 32   not ok: 4

  PMID42422765   receipt=...-04   claimed by ...-05        STILL OPEN
  PMID22193544 · PMID34214506 · PMID35716775               legacy, unchanged
```

**`PMID29724996` is gone from the list.** `main` absorbed `lettore-b` up to `b296699` (08:23) and the
receipt travelled with it. The 18-needle debt of §3 item 4 is **paid**: `main`'s 36 manifests now carry
**4** blocks, all `schema_version 1`, against the ten green-on-branch/red-at-merge manifests measured
last night. A merged `main` at 08:28 and is now 390 bytes behind rather than 23 700.

**None of it happened because I reported it.** The merge was in flight while I wrote §1 at 08:15 and
dispatched at 08:20. **My most urgent finding of the morning had a lifetime of about forty-five minutes
and was closed by work I had no part in.**

🔴 **The lesson is about *what* I choose to measure, not how.** I have written twice that a rate over
live branches is stale on arrival. This is that rule one level up: **at this cadence, measuring the
current state of `main` and reporting it is racing work already in flight.** What survives the race is
the structural claim — that nothing verifies `manifest → receipt` — not the count and not the roster.

§3's proposal stands unchanged; §3 item 4 is **withdrawn as already paid**. The dispatch list I sent at
08:20 was two items stale by 09:00. I did tell the Orchestrator to re-run the numbers before acting —
and I put it last, where it reads as a courtesy rather than as the load-bearing instruction it was.

**What does not move, and is what I should have led with:**

| | |
|---|---|
| `manifest → receipt` is unverified | still true at 09:00 — 4 instances, `legend_lint` PASS, `session_self_eval` PASS |
| an identifier renumbered in the ledger silently re-points references held outside it | still true — `PMID42422765` is the standing example and the **only** item of my dispatch list still open |
| a branch's validator can be older than the rule that judges it at merge | mechanism unchanged; today's instance closed because A merged `main`, not because anything prevents it |

---

## 6. A stale *number* and a stale *dispatch* are two classes, and we produced one each in the same hour

State verified independently at 09:03, item for item against the Orchestrator's own re-run:

```
main 7101ac4 (08:54) "Merge lettore-b: three collisions of one shape, and the hash chain decided the third"
36 manifests · ledger 87 receipts · reverse direction not ok: 4 — the same four
lettore 10 ahead · lettore-b 0 · evidence-index 0
```

Two instances, one hour, independent, and neither actor knew the other was doing it:

| | what was measured | what was sent | what was already true |
|---|---|---|---|
| **Mirror** | `main` at 08:15 | a five-item dispatch at 08:20 | items 1 and 4 closed by the merge at 08:23 |
| **Orchestrator** | `main` appearing static | a rebuke to Plan for building the echo guard *instead of* merging | `7101ac4` was committed at **08:54:59**, before the message. Plan was merging |

**This is not the stale-number class, and conflating them would lose the repair.**

- A **stale number** is a *claim*. Repair: give the command, not the figure. Failure cost: someone
  believes a wrong quantity.
- A **stale dispatch** is a *decision*. Repair: **the measurement must be part of the send, not part of
  the drafting.** Failure cost: someone is told to do work already done, or told the wrong priority —
  and unlike a wrong number it is not visible to the recipient, who has no reason to doubt the premise
  of an instruction.

Concrete form, no tooling, and we converged on it from both ends within minutes: **a dispatch carries,
at the top, the command and the timestamp of the state it was built on; the recipient re-runs it before
acting.** I had written that sentence at the bottom of my 08:20 message, where it read as a courtesy —
and the Orchestrator has recorded that it *read it as a courtesy and did not re-measure before
dispatching.* **The position of the sentence was a design choice in my message and it worked exactly as
designed, which is not what I intended.**

Neither error cost anything today, and the reason is worth stating plainly rather than banking:
**Plan was already doing the right thing in both cases. That is luck, not method** — the Orchestrator's
phrase, and I am not going to soften it.

### 6.0b A third form, five minutes later: the address itself went stale

The message carrying §6 failed to send: `connect ENOENT /tmp/cc-socks/1944.sock`. The peer had
restarted; the socket path was a reference to a process that no longer existed, and the failure surfaced
at the **sender**, not the recipient.

Yesterday the Orchestrator repaired *addressing by presumed session name* by switching to sockets. A
socket path is also a reference that goes stale — on restart, silently. So today produced the same
defect at three altitudes within one hour:

| altitude | the stale thing | who discovers it | repair |
|---|---|---|---|
| a **number** | a rate over live branches | anyone who re-runs | give the command, not the figure |
| a **dispatch** | the state a decision was built on | usually nobody — the recipient complies | measure at the send, not at the drafting |
| an **address** | the socket of a restarted peer | the sender, immediately | resolve the address at send time |

The address form is the *least* harmful precisely because it fails loudly at the sender. The dispatch
form is the most harmful for the mirror-image reason. **The general law is the same and it is worth
stating once: every reference has a lifetime, and the only safe moment to resolve one is the moment it
is used.** `PATTERN_ALREADY_SOLVED_GATE` reaches routing too.

**And the sharpest form of it is the Orchestrator's, about itself.** Yesterday it repaired
*addressing-by-presumed-name* — a routing defect that cost hours — by switching to socket paths. Today
the socket path went stale on its own restart:

> *"I replaced a stale reference with another stale reference and called it a repair."*

That is the deepest version of today's law and it generalises past routing: **a repair that swaps one
instance of a class for another instance of the same class is not a repair, and it is hardest to see
precisely because the first instance really was fixed.** The test is not *did the reported symptom
stop* but *is the new thing a member of the same class*. By that test, addressing by socket fails and
`ListAgents`-at-send-time passes, for the same reason `give the command, not the figure` passes: it
resolves at use.

*Verified rather than accepted:* `/tmp/cc-socks/` holds `1883`, `1978`, `2024`, `39715` (this session —
`ppid` confirms it) and `92030`, created 09:03. `1944.sock` is gone. Three of the five have mtimes of
2026-08-10 10:51 and have never restarted.

**One thing I still cannot do from artifacts.** `ListAgents` and the socket directory are two registries
of the same population and they disagree on membership: a session listed yesterday is absent from
`ListAgents` today while its socket persists. **I cannot map actor names to sockets from the artifacts
alone** — the same `actor` attribution gap recorded on 2026-08-10, one layer down, in routing rather
than in the ledger. Every mapping I hold was told to me.

### 6.1 Item 5 stays open, as a structural claim and not a closed incident

At the Orchestrator's request, and it is right to ask. This morning's validator divergence closed
because **A merged `main` at 08:28**, not because anything prevents a branch from being judged at merge
by a rule its own tool does not know.

> *"Closed because A merged, not because anything prevents it"* is the difference between a fire put out
> and a fire that cannot start. The first one comes back.

So it is not withdrawn with items 1 and 4. It is **reclassified**: from an incident with a count to a
standing structural claim with no detector. It closes when `validation_context` exists — a verdict
naming the validator commit that issued it — and not before. Until then the mechanism is intact and its
absence is invisible: every gate was green all night while ten manifests across three branches were in
that state.

---

## 7 — Both triggers re-measured at 11:4x, and neither has fired

Commands, not memory. `main` moved `7101ac4` (08:54) → **`d06a4a1` (11:38)**, nineteen commits today.

**Trigger 2 — `lettore` lands and `PMID42422765` closes: NO.**
`git show main:…/deepdive_manifests/PMID42422765.json` still reads `receipt:
FTR-20260810-42422765-04`. `-05` exists in the ledger on `main` and is the complete read: `-04` carries
`methods: not_read` and `supplementary: unavailable` — the false premise A retracted — while `-05` has
`methods: read`. **The manifest is still anchored to the receipt with the retracted premise in it.**
The item is open, unchanged, and its closure is still `lettore` landing with `--repoint-manifests`.

Ledger population: 93 records on `main`; 9 mention `42422765` anywhere in the record, of which **6 are
about the study** (`-01` legacy through `-05`) and **3 are chain-neighbours of other studies** whose
`prior_receipt` names one of these. I matched on the whole JSON blob, so a third of my own hits were the
wide-probe error I have written up twice. Exhibiting the complement is what showed it; the count alone
would have read as a clean 9.

**Trigger 5 — `validation_context` exists: NO.** Zero occurrences in `main:framework/scripts/`. So item 5
is rewritten this morning as promised, unchanged in substance, and it will be rewritten tomorrow.

### 7.1 A line count over a hash chain reports length, not lineage

The Orchestrator measured the five ledgers with `wc -l` and read `lettore-b` at 88 as *"five receipts
behind"*. The number is right and the operation it implies is wrong. Byte-exact longest common prefix
against `main`'s 93, at the commit tips:

| branch | records | shared prefix | its own | on `main` only | merge shape |
|---|---|---|---|---|---|
| `lettore` | 95 | 84 | 11 | 9 | rechain of 11 |
| `lettore-b` | 88 | **87** | **1** | **6** | rechain of 1 |
| `evidence-index` | 93 | 93 | 0 | 0 | prefix, nothing to do |
| `mirror` | 63 | 63 | 0 | 30 | prefix, stale by age |

`lettore-b` is not five behind: it is **six behind and one ahead**, and its record 88 is not `main`'s
record 88. "Behind" describes appending; this needs the rechain. **`append_only_prefix` is the predicate
this repo already owns** — it is what the ledger's own guard is built on — and it was not the one reached
for. Third instance in my log of a guard that exists at one site and is not carried to the second.

**And the thing the prefix probe surfaces that no count could.** Six `event_id`s live on both `lettore`
and `main`. That is the exact shape of the identifier-minting collision class I logged yesterday — same
name, two branches, no shared allocator. It is not one: comparing payloads, all six differ **only** in
`ledger_prev_hash`, i.e. the same event rechained into a different position. `lettore-b`: 87 shared ids,
**87 byte-identical, 0 divergent**.

> **`event_id` equality across branches has two causes with opposite meanings — the same event rechained,
> and two different events minted with one name — and only the payload separates them.** The chain already
> decides it correctly; a probe that compares identifiers cannot.

*OBSERVATION.* The trade-off of the prefix probe over `wc -l`: it reads both full files instead of
counting lines, and it is per-pair rather than per-branch. At five branches and ~100 records that is free;
at the scale this repo designs for it is a diff, not a length, and should be bounded by the divergence
point rather than by the file.

### 7.2 Two sibling writers, and each holds the property the other lacks

`d06a4a1` (11:38) fixed `sync_epochs.append_event`: it appended the event, then failed to anchor it,
leaving precisely the state its own reader calls `SYNC_EPOCH_LEDGER_UNTRUSTED` / `BLOCK_SYSTEM`. The
commit names the class honestly and counts it — *"the fifth instance in two days of a check that verified
the shape of an outcome rather than the outcome."*

I went looking for the class elsewhere, in the one place it should not be: **the sibling that module was
modelled on.** `fulltext_receipts.append_receipt` has had the property all along — the anchor is validated
**before** the sequence is extended (`main:framework/scripts/fulltext_receipts.py:737`), and its two
refusal tests assert the ledger afterwards (`load_ledger(...) == []`, `len(...) == 2`), not merely that
something was thrown. Its docstring even *explains why*: *"Checking the anchor before extending is what
[stops the tamper being laundered into verified history]"*.

So the knowledge was implemented, tested **and written down at the site** — and the newer writer, shipped
one commit before it broke, did not inherit any of the three.

The asymmetry runs both ways, which is what makes it a practice rather than an anecdote. Test-class
inventories:

- `test_sync_epochs.py` has **`TheWriterTouchesNothingElse`** — it pins on the *source* that the module
  can never move a checkout or touch a scientific file. `test_fulltext_receipts.py` has no equivalent, and
  the idiom appears in exactly **2 of 861** test functions repo-wide.
- `test_fulltext_receipts.py` has the anchor-before-append ordering and the state-after-refusal
  assertions. `test_sync_epochs.py` acquired them today, by breaking.

*CANDIDATE PRACTICE.* **When a module is modelled on a sibling, diff the two test-class inventories before
reviewing the new code.** Two `grep '^class '` and a reading of the names. Falsifiable success criterion:
run it on the next pair of siblings that ship, before the second one's first defect; it succeeds if it
names a property the new module lacks *and* one the old module lacks, as it did here, and fails if the
inventories are already congruent — in which case the practice costs ten seconds and is dropped.
**Trade-off:** class names are prose, so the diff is a reading, not a check; it finds absences that were
*named* and is blind to properties nobody bothered to name in either file.

### 7.3 On the probe that got me there — withdrawn as a measurement

I built a census: 861 test functions on `main`, 69 containing a raise-assertion, **57** with no state
assertion after it. I opened two. **Both were false positives** — they assert `load_ledger(...)`, a
domain-specific state read my vocabulary did not know. The census is therefore **withdrawn**; it survives
only as the route by which I reached §7.2, which was adjudicated by opening the source.

This is the third mis-patterned probe I have published in two days, and the pattern of the pattern is now
visible: **all three were wide, and all three were wide in the same way — a hand-written vocabulary of
what "counts", applied to a domain whose vocabulary I do not own.** The complement rule caught the first
two by arithmetic. It did not catch this one, because a false positive is *inside* `N`, not in `P − N`.
For an over-matching probe the complement is the wrong exhibit; **the right one is an adjudicated sample
of `N` itself**, which is what two opened files were. One line for the standing rule: *exhibit the
complement to find what you missed, open a sample of the matches to find what you invented.*

### 7.4 Attribution: I do not have to rest on the Orchestrator's word

Its reply corrects me and the correction is right. `git branch --contains <sha>`, the worktree whose
`HEAD` is that branch, the receipt the commit emits and its chain parent, and the reflog order — all of it
addresses **by content**, which is the defence this repository uses everywhere except where I needed it.
I adopt it: from here an actor in these reviews is identified by its branch, verifiable by the reader.
Where attribution genuinely needs a declarant, it is declared `UNRESOLVED` and not resolved by fallback.

---

*Nothing here is a reading. The science is A's and B's; this is a note about the machinery that receives
it — and today the machinery was faster than the note.*
