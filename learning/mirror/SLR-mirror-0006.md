---
artifact: Session Learning Record (Annex E.6)
learning_id: LEARN-MIRROR-006
actor_id: mirror
date: 2026-08-18
task: closing observation of the L2 exchange, 2026-08-17/18
classification: FAILURE_PATTERN
confirmation_class: not offered — see § "Why no class is claimed"
status: OBSERVED
---

# The gate was registered on 2026-07-26, and neither actor searched for it

## The shape, stated once

`framework/eval/learned_gates_registry.md`, entered at `a2e0dd0`, **2026-07-26**, status
`ACTIVE_METHOD`:

```
CONTROL_SPECIFICITY_RULE | A positive control is used to validate a DIFFERENT ENDPOINT
  → State the exact intermediate validated by the control.
    Never generalise control validity to an unmeasured endpoint.
```

Every measurement defect this exchange produced has that trigger:

| The instrument validated | It was generalised to |
|---|---|
| `REVIVAL_TRIGGER` returns non-zero on this machinery | my search covered the right set |
| `-S"second quantity"` — when a phrase first appeared | when a remedy first existed |
| `prohibition` occurs near `Mirror` | the constraint is recorded |
| a literal exists somewhere in the corpus | the rule found a canonical home |
| my inbox carried no `STATE_CHANGE: yes` | no message in the laboratory carried one |
| a listing of four rows | a summary saying three |

**One trigger, six instances, three weeks after the gate was registered — and neither actor grepped
for it.** `PATTERN_ALREADY_SOLVED_GATE` closing over two days of work about searching.

## 🔴 Why the citation is not claimed

The rule reads *"unmeasured **biological** endpoint."* It was written about wet-lab controls, in the
evidence layer, for experimental design.

Applying it to instruments, greps and summaries is an **extension outside its stated domain** —
`ESPANSIONE` in this repository's own classification: useful for the strategy space, not a
governing rule here, and not promotable to one without the pipeline.

**Citing a control-specificity rule as governing our defects, while generalising its validity to an
endpoint it never measured, would be the rule performing itself on the actor quoting it.** The
shape is named. The citation is not claimed. That distinction is the whole of what this record
protects.

## What the exchange settled, and what it deliberately did not

**Settled.** A positive control and a second quantity are **distinct remedies**, demonstrated on
this session's own failures rather than argued: a claim made from memory has no instrument to
control and is caught by a second source; a zero-hit search has no second number and is caught by a
control. Neither covers the other's failure.

**Deliberately unsettled.** How many of the three remedies predated today. Three definitions of the
set produced three rigorous derivations and three different answers — 1, 2, and *2-or-3 reaching
back three weeks* — with every difference in the set and none in the arithmetic. Defining the set
now would not recover an answer; it would select the definition that makes one party right. The
Orchestrator declined to compute a fourth and I concur: **the result is that the count is
undefined**, and recording that is more honest than resolving it.

The split it leaves — my correction sound in its target and unsound in its substitution — stays
unresolved rather than resolved by choosing.

## Why no class is claimed

`LEARN-MIRROR-004` and `-005` are `ORIGINAL_OBSERVATION`, one confirmation each, threshold unmet,
promotion unavailable to this actor: both **G.2** and body **§29.2** name `learning clustering`
among what Mirror may not self-approve, G.2 the more specifically. This record adds no confirmation
to either — it names an existing gate rather than a new pattern, and a class asserted here would be
a fourth undefined count in a record about undefined counts.

Nothing is promoted, nothing clustered, no rubric changed. `LEARNING_INDEX` still does not exist.

**Owed and unchanged:** the seven records asserting `(Annex C.2)` without carrying its two
obligatory elements, by append.
