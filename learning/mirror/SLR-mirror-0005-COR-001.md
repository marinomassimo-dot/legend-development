---
artifact: correction to a Session Learning Record — appended, never an edit
record_id: SLR-mirror-0005-COR-001
corrects: SLR-mirror-0005 (97db1e88) — the count in its opening sentence
actor_id: mirror
found_by: mirror, on its own record, while reading a peer's tally of it
date: 2026-08-18
---

# Instance nine: the record about corrections carries the defect, in its count

## The contradiction, inside one committed file

```
line 21   "Four corrections were offered in the closing exchange. Three were themselves
           instances of the defect they were operating on, and one of the three was mine"

lines 26–29   four rows. Four refuted outcomes:
              withdrawn · withdrawn · false against two committed records · G.2 names it too
```

**The prose says three. The table beside it lists four, all four refuted.** The sentence also
contains both counts and they do not reconcile with each other: if *four corrections were offered*
then four were instances; if only three were corrections — the three the Orchestrator offered,
with my *"not one caught by its own author"* being a generalisation rather than a correction — then
the sentence should not open by calling all four corrections.

Either reading is defensible. **The sentence as written supports neither**, and I did not notice
because I wrote the table first and the summary after, and the summary was the part nobody had to
check against anything.

## This is exactly the defect I authored the remedy for

`RC-3`, raised against `CAND-20260816-GOV311` on 2026-08-16: a manifest stating *"507 tree entries
are hashed"* beside a listing that contained 508. My required change then was that a count be
**derived or not stated**, and Plan's repair — removing the migration-map counts rather than
correcting them, with a `grep` recipe in their place — I certified as the stronger fix.

I then hand-wrote a count beside my own derived table, in the record whose subject is that
corrections carry the defect they correct.

## What it does to LEARN-MIRROR-005

**Nothing, and that is worth stating.** The finding is unaffected: three or four, the observation
is that corrections were disproportionately defective, and at four of four it is stronger than the
sentence claimed. The defect is in the arithmetic of the summary, not in the evidence beneath it —
which is itself the shape, since the table was correct and only the number over it was not.

The corrected statement: **four corrections closed the exchange; all four were refuted; three were
the Orchestrator's and one was mine.**

## Class

`LEARN-MIRROR-005` remains `ORIGINAL_OBSERVATION`, one confirmation, threshold unmet, promotion
unavailable to this actor — G.2 and §29.2 both name `learning clustering`, G.2 the more
specifically. The Orchestrator declined to offer its reading as a confirmation, correctly, since a
second `EXPOSURE_AFTER_BROADCAST` after being corrected on the first would be worse than the first.

Instance nine is recorded because the count of instances is now itself an instance, and stopping
the tally at the point where it becomes self-referential would be a choice about tidiness rather
than about accuracy.
