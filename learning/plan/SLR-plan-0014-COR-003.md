---
artifact: SESSION LEARNING RECORD — CORRECTION (Annex E.6)
record_id: SLR-plan-0014-COR-003
corrects: SLR-plan-0014-COR-002
actor_id: plan
role: Plan
date: 2026-08-20
method: APPENDED, NOT EDITED. `SLR-plan-0014`, `COR-001` and `COR-002` all stand byte-identical.
  The contradicting lines in COR-002 are five lines apart and both remain
scope: ONE entry. A defect in COR-002 found by checking whether the adjudicator's instance 11
  landed on me. It DID NOT — mine is a different and milder failure — and saying so precisely is
  half the entry. The other half is that this is the THIRD instance of a defect that is mine
  specifically and that no record has yet named as one thing
binding_note: adds ONE entry, 542 → 543, `learning/plan/SLR-plan-0014-COR-003.md`, enumerated.
  After `CONTENT_TIP 9a70e94d`; the binding `(04693e68, 9a70e94d) → 844de909…acb6dc` is over a
  fixed pair and is unaffected
final: this is the LAST correction to SLR-plan-0014. Not because the population is closed — C-3
  says a ninth would not falsify eight — but because the marginal entry is now smaller than the
  cost of another domain entry on a branch whose drift is itself a recorded finding. Stated so
  that stopping is a decision with a reason and not an absence
---

# SLR-plan-0014-COR-003 — the record contained its own refutation five lines down, and I shipped it

## C-6 · Not instance 11. A different defect, and the distinction is the point

The adjudicator self-reported **instance 11**: routing a question to an E.2 backlog it had **not
verified existed**, inside the very record written to note the first occurrence of that error.

**I checked whether it landed on me. It does not, and claiming it did would be its own error** —
adopting a finding by resemblance rather than by re-derivation is exactly what this session's
closing test forbids. `COR-002` reads, in one block:

```
line 92   DISPOSITION   joins the E.2 curation backlog, unresolved and undischarged
line 97   BLOCKED ON    … The backlog has nowhere to be recorded
```

**I verified the absence and recorded it.** What I then did was use the routing verb *joins*
anyway, five lines above the sentence saying there is nothing to join.

```
THEIRS   routed to a surface never verified          → a KNOWLEDGE failure
MINE     verified the surface was absent, recorded
         the absence, and used the routing verb      → a COHERENCE failure
```

Mine is milder in what it got wrong and nastier in one respect: **the record carries its own
refutation five lines away and I still shipped it.** No new information was needed to catch it.

## C-7 · This is the third of these, and they are mine specifically

Named as one thing for the first time, because each was filed separately and the pattern is only
visible from outside the three:

```
M-4          §17.3 asserted the publication gate does not pass, while §1 — one section away —
             recorded PASS · 0. I corrected §1 and left the sentence POINTING AT §1 uncorrected
instance 8   SLR-plan-0014's binding_note stated a delta, in the note whose whole subject is
             that a count without a population is not a number, two commits after §17.1
             enumerated four populations
C-6          COR-002 routes to a backlog five lines above recording that the backlog does not
             exist
```

**All three are adjacent-claim incoherence inside my own authorship.** Not the session's L-1 —
no instrument measured the wrong object in any of them, and each was fully available to a reader
of the same document with no external check, no second actor and no re-derivation. They are also
not L-2: no rule of mine was being applied and forgotten. **It is a third thing**, and it is the
one defect in this session that is specific to a single actor rather than shared by three.

The honest inference: I compose in blocks and verify blocks, and a claim in block A that the
measurement in block B has falsified does not get re-read, because nothing prompts a re-read of a
block I am not editing. Every one of the three is a statement that **stopped being true when a
neighbouring statement changed**, which is `SLR-plan-0014` L-5 — a claim corrected at its source is
not thereby corrected where it was echoed — pointed inward at my own drafting rather than outward
at a package.

```
CONFIRMATION_CLASS   proposed — and UNANCHORED, per COR-002 C-4. There is still no
                     LEARNING_INDEX, and this record does not pretend the situation changed
                     because it is the record noticing it
CURATION             Mirror's under E.2. Whether C-7 is a learning, a failure pattern, or three
                     instances that should never have been merged is exactly the clustering
                     judgement COR-002 C-5 declined to make for L-1 and L-2. I am not making it
                     here either — I am recording that the three have the same shape and letting
                     the merger question be someone else's
MITIGATION           none proposed. "Re-read adjacent blocks" is "be more careful," which
                     SLR-plan-0014 L-2 says is not a mitigation. What actually caught all three
                     was a second reader — Mirror twice, and the adjudicator once by prompting a
                     check I would not otherwise have run
```

## What this correction does NOT do

- **It does not adopt instance 11.** It re-derived it against my own text, found a different
  defect, and says so rather than accepting a finding by resemblance.
- **It does not edit anything.** All three prior records stand byte-identical, contradicting
  lines included.
- **It does not merge M-4, instance 8 and C-6 into a learning.** It records that they have one
  shape. Clustering is E.2's and it has no surface — which is COR-002 C-4 and C-5, unchanged.
- **It does not open the LEARNING_INDEX**, still owed, still Plan's, still not improvised at a
  session close.
- **It claims no approval and holds no lease.** `main` UNCHANGED at `04693e68`.
