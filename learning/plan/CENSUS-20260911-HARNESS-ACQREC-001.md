# Attribution census — `HARNESS-ACQREC-001`, 2026-09-11

**Actor:** `plan` · **Task:** `HARNESS-ACQREC-001` · **Recorded by:** `orchestrator`

**Provenance.** Persisted verbatim from the actor's closing report, under §21c's safe default for
reports that exist only in a transcript; the same block is in the task JSON. Not re-graded.

**Task:** the dependency screen as a manifest slot with ratchet semantics; census P2 — acquisition
recipes, `reacquire.py`, a versioned retrieval manifest, the PyMuPDF pin.

**The actor's own reading.** Its first version emitted the two new absences as blocking declared
gaps, which refused every historical manifest and turned five lint fixtures red — caught by the
coordinator from the suite, and the actor counts it as the peer catch. Machine: the release gate
refused a literal e-mail address in a URL; the provenance suite demanded the AGPL notice for the
pin. Self: an argument order in its own self-test and a mutant whose assertion was wrong before the
first green. First replay on this host: 26 of 32 recipe-bearing artefacts recovered, both predicted
failures measured and dated rather than rediscovered.

```
ATTRIBUTION_CENSUS
incidents: 4
machine: 2   blind_auditor: 0   peer: 1   self: 1
severity_high: 1   of which self: 0
undetected_known: 0
```
