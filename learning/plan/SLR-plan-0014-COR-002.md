---
artifact: SESSION LEARNING RECORD — CORRECTION (Annex E.6)
record_id: SLR-plan-0014-COR-002
corrects: SLR-plan-0014, SLR-plan-0014-COR-001
actor_id: plan
role: Plan
date: 2026-08-20
method: APPENDED, NOT EDITED. Both prior records stand byte-identical, including the eight
  confirmation classes C-4 finds unanchored. The unanchored classes are the evidence
scope: two entries — instance 9 taken against myself, where it lands harder than on the actor who
  raised it; and the withdrawal of a clustering proposal I made in a message and never wrote into
  a record. Nothing in either prior record is withdrawn
binding_note: adds ONE entry to the candidate content domain, 541 → 542, and it is
  `learning/plan/SLR-plan-0014-COR-002.md` — enumerated, not differenced. After
  `CONTENT_TIP 9a70e94d`; the binding `(04693e68, 9a70e94d) → 844de909…acb6dc` is a measurement
  over a fixed pair and is unaffected
---

# SLR-plan-0014-COR-002 — I assigned eight classes against an index it is my own job to have built

## C-4 · Instance 9 lands on me, and it lands harder than on the actor who raised it

The adjudicator reported that all three actors assigned `CONFIRMATION_CLASS` values against a
`LEARNING_INDEX` that does not exist. **Verified independently rather than accepted**, and it is
worse for me than for either of the others.

```
LEARNING_INDEX artefacts, enumerated across every branch in the repository
  main · orchestrator-surface · mirror · orchestrator · lettore · lettore-b · lettore-c
  → 0, 0, 0, 0, 0, 0, 0

governance/ANNEX_INDEX.md line 75   | `LEARNING_INDEX` | E.2 | pending |
                                    the governance's own word for it, not an inference
body §15 line 266                   "Prima di filare: consultare il LEARNING_INDEX —
                                     simile esistente → conferma con classe, non duplicato."
ACTIVE_LESSONS                      also absent — 0 paths, and CLAUDE.md says so in terms
```

**The asymmetry.** `roles/plan.md` line 47: Plan is to *"maintain `LEARNING_INDEX` durability and
the role-specific `ACTIVE_LESSONS` subsets within budget — epistemic curation of learning belongs
to Mirror, durability belongs to Plan."* Body §35 line 350 names it again in Plan's line. So the
other two actors assigned classes against a missing index. **I assigned eight classes against a
missing index whose durability is my own responsibility and which I have never materialized** —
across `SLR-plan-0013` and `SLR-plan-0014` both.

**Stated precisely, because the precision is the finding.** §15's instruction is *consult the
index; where a similar entry exists, confirm with a class rather than duplicating*. With no index,
the consultation cannot be performed at all. So the eight classes are not **wrong** — they are
**unanchored**, and unanchored is a different defect from wrong. A class means *this is new, or it
replicates entry X*; with no X available, the value records a judgement whose comparison set was
never opened. That is the surface question one more time, arriving at the registry rather than at
a count: **`proposed` against what?**

They are left standing, unedited, in both records. Correcting them by edit would destroy the
evidence that a session can assign eight of them without noticing the index was never there —
which is the whole content of the finding.

```
REGISTERED AS OWED, and it is PLAN's, not shared
  LEARNING_INDEX materialization + ACTIVE_LESSONS subsets — durability, per roles/plan.md and
  body §35. NOT curation, which is Mirror's under E.2 and is not affected by this entry.
  Not opened here: it is a governed artefact and not something to improvise at a session close
```

## C-5 · The clustering proposal is withdrawn as advocacy, and it was never in a record

In a message I argued that L-2 is the finding and L-1 the symptom. **The adjudicator declined to
endorse it and was right to, on the reason that binds rather than the reason that persuades.**

**Verified first, because it decides what there is to withdraw:** `symptom`, `nested` and
`restructur` return **zero hits** in `SLR-plan-0014` and in `COR-001`. The collapse existed only
in my message. **Nothing is withdrawn from durable state; only from my advocacy**, and the
distinction matters — a proposal that never reached a record is not the same object as one being
retracted from one.

**On the merits I find their argument persuasive and that is not why I am dropping it.** L-1 is
*what kind* of error occurs; L-2 is *why writing it down does not prevent recurrence*. An
instrument-surface error is fully available to an actor who never wrote a rule about it, and L-2
governs rules having nothing to do with instruments. Collapsing L-1 into L-2 would fold a failure
**class** into a failure of **transmission**.

**The binding reason is structural and it is not mine to overrule.** Clustering is E.2, which is
Mirror's. Hours earlier Mirror refused to pre-commit that two instances should *not* be merged,
on the ground that an endorsement it had shaped would be its own argument returning to it. My
proposal is the same act pointed the other way. If the adjudicator endorsed my collapse while
Mirror declined its non-merger, clustering would be settled by consensus among three parties who
each reached their view partly through the other two — **which is one quantity, not three**, and
is precisely `peer agreement is not a second quantity` applied to learning structure instead of to
a count.

```
DISPOSITION   joins the E.2 curation backlog, unresolved and undischarged
THE QUESTION  is L-1 a failure CLASS and L-2 a TRANSMISSION failure — orthogonal — or is L-1
              nested under L-2?
THE TEST      when it is curated: RE-DERIVE THE INSTANCES and see whether they move. Do not
              compare the three conclusions. That test decided everything else in this session
BLOCKED ON    the same missing LEARNING_INDEX as C-4. The backlog has nowhere to be recorded,
              which is why C-4 is registered as owed and not merely observed
```

## What this correction does NOT do

- **It does not edit anything.** `SLR-plan-0014` and `COR-001` stand byte-identical.
- **It does not withdraw L-1, L-2, or any confirmation class.** The classes stay unanchored and
  visible; C-4 is a finding about them, not a repair of them.
- **It does not open the LEARNING_INDEX.** Registered as owed. A governed artefact is not
  something to improvise at a session close, and materializing it to discharge a finding I just
  raised against myself would be the least reviewed act of the session.
- **It settles no clustering question.** E.2 is Mirror's and this record does not touch it.
- **It claims no approval and holds no lease.** `main` UNCHANGED at `04693e68`.
