# Attribution census — `HARNESS-DEPINTEG-001`, 2026-09-10

**Actor:** `plan` · **Task:** `HARNESS-DEPINTEG-001` · **Recorded by:** `orchestrator`

**Provenance.** Persisted verbatim from the actor's closing report by the coordinator that
received it, under §21c's safe default *"a report that exists only in a transcript → persist
verbatim, note the source"*. The actor had closed; the block below is its own count, not the
coordinator's re-derivation, and it is not re-graded here. `session_self_evaluation.md` Part 3
requires this block in the diagnosis itself — these three tasks predate that requirement by
hours, which is exactly the gap this file exists to stop widening.

**Task:** census P1, dependency-integrity propagation.

**The actor's own reading:** five of eight went to the machine and two of those mattered —
its suite caught a screen returning CLEAN without screening anything, and the release suite
caught a second copy of a canonical heading pattern that was *producing the right answer*,
which is how a second copy survives long enough to drift. Reader practice caught only the
unverified licence. Its one `undetected_known` is the honest one: a Crossref deposit may
carry only some of a paper's references, so a partial deposit reads as full coverage of a
smaller denominator, and no measurement here can see it.

```
ATTRIBUTION_CENSUS
incidents: 8
machine: 5   blind_auditor: 0   peer: 0   self: 3
severity_high: 3   of which self: 1
undetected_known: 1
```
