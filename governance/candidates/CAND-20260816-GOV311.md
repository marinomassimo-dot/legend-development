---
artifact: INTEGRATION_CANDIDATE manifest (Annex D.2)
candidate_id: CAND-20260816-GOV311
governance_version: 3.1.1
change_class: MAJOR
prepared_by: plan
prepared_on: 2026-08-16
state: READY FOR MIRROR HOSTILE REVIEW
---

# INTEGRATION_CANDIDATE — Governance v3.1.1

## 1 · Manifest (Annex D.2)

```yaml
CANDIDATE_ID:               CAND-20260816-GOV311
REVISION:                   5
BASE_HEAD:                  749a9a9b8f29c855f803a43b979c591532557561
BRANCH:                     evidence-index
BRANCH_TIP:                 9720a0cd1dddfe457f8e4eec1ebe47bd627512cc   (tree defining the domain)
MANIFEST_COMMIT:            `git rev-parse evidence-index` at delivery — control plane, outside
                            the domain, and unrecordable inside the file it names
CANDIDATE_CONTENT_HASH:     c39ecae89677363802c8c7d24b704da185fc568fed360b08ad01adb39730c239
CANDIDATE_HASH_VERSION:     legend-candidate-v3   (defined in plan_defined_parameters.md § P5)
CHANGE_CLASS:               MAJOR
LINT_RESULT:                PASS
PUBLICATION_GATE:           PASS / BLOCKS: 0
PRIOR_ART_SOURCE_SHA256:    2563f82e82d661f98ff5eb2b029a8568b519d0ac3ae91e4fd76ba21704d6988e
PRIOR_ART_ARCHIVED_SHA256:  2563f82e82d661f98ff5eb2b029a8568b519d0ac3ae91e4fd76ba21704d6988e
BYTE_IDENTITY:              PASS
PLAN_FINGERPRINT:           37c3b863a88db79c209b6aeb73366d7d498519d4cd87c5a6b6a760340e15495f
MIRROR_REVIEW:              PENDING — revision 4 returned REVISION_REQUESTED (c6a290e1)
HUMAN_APPROVAL:             PENDING
SNAPSHOT_ID:                n/a until canonical execution — GATE 4 belongs to Orchestrator
```

🔴 **Four earlier hashes are SUPERSEDED — DO NOT REVIEW:**

| Hash | Revision | Why superseded |
|---|---|---|
| `f869a523…f8b8909` | 1 | contained an undeclared new rule in `parallel_legend_protocol.md` |
| `3e4cf135…b6e05474` | 2 | predates the arrival of the prior-art design record |
| `57fde97b…8685ba3b` | 3 | carried a reconstructed transcription of the design record, wrong in 49 of 151 lines |
| `2e7da13f…9256d7` | 4 | computed by a command that was never published; Mirror `REVISION_REQUESTED` (RC-1…RC-5) |

> **Revision 5 clears nothing.** Mirror reviewed only **R-1 (binding)** and parked
> **R-2 through R-7**. This revision must return for a **new review from the beginning**; no
> verdict, note or confidence from `REV-GOV311-MIRROR-001` transfers to this hash.

`CHANGE_CLASS: MAJOR` is not a judgement call: Annex D and body §12 make governance, authority
and gate changes MAJOR by definition, and this candidate is all three.

`BASE_HEAD` is unchanged from revision 1. `main` is still `749a9a9b`, the branch is 0 commits
behind, and the GATE 0 base condition holds without a further alignment.

### How the hash is defined — by reference, not here

**The single authoritative definition is
[`plan_defined_parameters.md` § P5](../plan_defined_parameters.md).** This manifest references
it; it does not restate it. That is the repair for RC-2, where a `v1` definition in P5 and a `v2`
recipe in the manifest disagreed while PID-06 named P5 as the implementation site.

`CONTENT_DOMAIN_DEFINITION` (P5.1, in one line): every path in the tree at `BRANCH_TIP` **except**
paths under a declared control-plane root — today `governance/candidates/` and `ledger/`. Content
is what is proposed for canonical integration and determines the candidate's identity; the
control plane is what *describes or manages* a candidate, a review or an actor's runtime state,
and cannot change the identity of what it describes.

`REPRODUCTION_COMMAND` — one command, from the repository root:

```bash
python3 governance/scripts/candidate_content_hash.py \
  --base 749a9a9b8f29c855f803a43b979c591532557561 \
  --tip  9720a0cd1dddfe457f8e4eec1ebe47bd627512cc --show-domain
```

It prints the version, base, tip, included entry count and **every excluded path**, then the
hash. The count is derived by the same pass that builds the value (RC-3), so it cannot go stale
beside it, and the excluded set is visible rather than trusted.

The script does not carry its own copy of the rule: it parses `CANDIDATE_HASH_VERSION` and
`CONTROL_PLANE_ROOTS` out of § P5, the same anti-drift shape as `governance_fingerprint.py`
(PID-03). Nothing passes through a shell — the revision-4 defect was `$(...)` stripping one
trailing newline.

The version prefix moves to `v3` because both the byte layout and the excluded roots changed, so
`v1` and `v2` values can never collide with these.

### Source commits

| # | Commit | Milestone |
|---|---|---|
| 1 | `318d1171` | Governance body materialized verbatim; annexes recorded as not transmitted |
| 2 | `ccdbe948` | Annexes A–J materialized; the seven delegated parameters defined |
| 3 | `a8cd1253` | Role contracts, `BOOTSTRAP.md`, deployment profile |
| 4 | `36305c61` | Fingerprint composition made executable; first A.6 checkpoint |
| 5 | `a34bf188` | Base alignment onto `main`; MAT-001's classification corrected |
| 6 | `04cbd3bd` | `CLAUDE.md` migrated to a router; every rule given a canonical home |
| 7 | `891fba91` | Candidate revision 1 prepared (MAT-007, CHK-plan-0002) |
| 8 | `ca225672` | Undeclared rule withdrawn; MAT-008, CHK-plan-0003 |
| 9 | `7f7c0cb0` | Candidate revision 2: hash definition made a rule; PID-17 declared |
| 10 | `1c755e59` | Prior-art design record archived as a transcription; MAT-009, CHK-plan-0004 |
| 11 | `d371eed4` | Candidate revision 3 |
| 12 | `3c6c6e15` | Byte-faithful source replaces the transcription; MAT-010, CHK-plan-0005 |
| 13 | `154aef5b` | Candidate revision 4 (the revision Mirror reviewed) |
| 14 | `9720a0cd` | Mirror remediation RC-1…RC-5; P5 amended, hash script, MAT-011, CHK-plan-0006 |

The manifest commit that carries revision 5 is outside the content domain by rule, so it does not
appear as a source of the hash and does not perturb it.

Design record (non-normative provenance): [`governance/design_records/`](../design_records/) —
[`prior_art_review_v3.1.md`](../design_records/prior_art_review_v3.1.md), a **byte-faithful copy**
of the source file taken from the filesystem (`BYTE_IDENTITY: PASS`, hashes in §1), carrying no
added header; plus `materialization_log.md` (MAT-001…010, append-only), `README.md` where the
classification and the proposed-vs-ratified divergences live, and `claude_md_migration_map.md`.

Revision 3 had archived a reconstructed transcription; it is superseded and the reason is
recorded in MAT-010 rather than erased.

---

## 2 · PLAN IMPLEMENTATION DECISIONS — PROPOSED, SUBJECT TO MIRROR HOSTILE REVIEW

Nothing below is text of the FROZEN Governance v3.1.1. These are the choices the frozen text
delegates to Plan, listed individually so Mirror can take them one at a time.

| ID | Decision | Delegating clause | Rationale | State | Implemented in |
|---|---|---|---|---|---|
| PID-01 | `RETRY_POLICY.on_exhaust` default = `PARK` | A.1 | `REASSIGN` spends a second actor on an undiagnosed failure; `ESCALATE` spends a HUMAN_REQUIRED that §3 counts as `PREVENTABLE`. `PARK` spends nothing and loses nothing. | PROPOSED | `plan_defined_parameters.md` § P1 |
| PID-02 | Fingerprint composition: per-role pertinence sets + hash function, `FINGERPRINT_COMPOSITION_VERSION: 1` | A.6, H.1 | Annex J split by section so a J.4 change cannot invalidate a scientist's in-flight reading — A.6's own worked example. Verified structurally: scientist resolves to J.0/J.2/J.3, orchestrator to J.0–J.4. | PROPOSED | § P2 + `governance/scripts/governance_fingerprint.py` |
| PID-03 | The composition script **parses** § P2.2 instead of copying it | A.6 | A script carrying its own copy of the sets is a second source of truth; the two diverge the moment one is edited. Reading the prose keeps the document authoritative and fails loudly if it stops parsing. | PROPOSED | `governance_fingerprint.py` |
| PID-04 | ACK timeout = 30 minutes | B.3 | An actor inside one long turn cannot see a message until it ends; a timeout below a legitimate working turn manufactures BLOCKERs, and a BLOCKER that usually means nothing is ignored when it means something. | **PROVISIONAL** — `PROV-ACK-TIMEOUT-30M`, expiry after the third scientific batch | § P3 |
| PID-05 | Heartbeat 30 min, DOWN after 3 missed (~90 min) | B.3 | A heartbeat costs an actor turn, so cadence trades against the work it interrupts; three misses keeps a long turn from reading as a crash, and DOWN is recoverable. | **PROVISIONAL** — `PROV-HEARTBEAT-30M-3X`, same expiry | § P4 |
| PID-06 | `CANDIDATE_CONTENT_HASH` defined **once**, in § P5, as SHA-256 over `legend-candidate-v3` + BASE_HEAD + the newline-terminated `ls-tree` listing of the content domain; computed only by `candidate_content_hash.py`, which parses P5 rather than copying it | D.2 | A tree listing is content-addressed over contents, modes and paths and carries no commit metadata — D.2's exact distinction. BASE_HEAD is inside the hash so an approval cannot be transplanted onto another base. **Amended at revision 5 (RC-1, RC-2):** revision 1 hand-picked a commit, revision 2 introduced a filter that lived only in the manifest while P5 still said `v1`, and the recorded value came from a shell form that stripped one byte. The definition now exists in one place and is executed by one script. | PROPOSED (amended twice) | § P5 · `governance/scripts/candidate_content_hash.py` |
| PID-19 | **Content vs control plane.** An artifact that *constitutes* the candidate determines its identity; an artifact that *describes or manages* a candidate, a review or an actor's runtime state cannot. Control-plane roots declared exhaustively in P5 as `governance/candidates/` and `ledger/`; adding one is a governed change to P5 | D.2; RC-5 | Revision 4 broke one fixed point and left an equivalent one standing: checkpoints sat inside the hashed tree, so recording progress on a candidate changed the candidate. A classification resolves it generally; an exception for one file would not. `--show-domain` prints every excluded path, so exclusion is auditable rather than trusted, and nothing scientific or normative can fall out by accident — `disease-models/`, `framework/`, `roles/`, `scripts/`, `deployment/`, the top-level documents and all of `governance/` except `candidates/` lie outside both roots. | PROPOSED | § P5.1 · `candidate_content_hash.py` |
| PID-18 | The design record keeps the path `prior_art_review_v3.1.md` rather than the source basename `LEGEND_v3.1_prior_art_review_matrice (1).md`, and carries **no header inside the file** | operator instruction; I.1 | The source basename holds a space and a browser's `(1)` deduplication suffix — download artifacts, not provenance. The bytes are identified by their SHA-256, recorded in the manifest, the README and MAT-010. Classification metadata lives outside the historical bytes, because a document annotated inside its own body is no longer the document that was written. | PROPOSED | `governance/design_records/prior_art_review_v3.1.md`, `README.md`, MAT-010 |
| PID-17 | The two sentences added to `parallel_legend_protocol.md` rule 4 — *read the diff against the target, not the ancestor* — are **withdrawn** from the protocol and registered as `PROV-DIFF-AGAINST-TARGET` | A.1/E.3 lifecycle; C.1 floor R4; migration step 2.3 | They were new law, not preservation: the source rule ends at *"in full, or not at all"*. Step 2 authorised relocating content into `framework/protocols/`, not adding to it. One `ORIGINAL_OBSERVATION` without Mirror validation is below E.2's threshold, and a methodology-changing rule carries an R4 floor. Rule 4 is now byte-identical to its source, verified by diff. | **PROVISIONAL** — expiry at Mirror's first coordination review | MAT-008; `framework/protocols/parallel_legend_protocol.md` (reverted) |
| PID-07 | ACTIVE_LESSONS budget = 25 lessons or 4 000 words | E.5 | A fixed cap is right *because* E.5 defines the overflow behaviour; a cap expressed as a fraction of something growing would let the subset grow forever while appearing bounded. | **PROVISIONAL** — `PROV-LESSON-BUDGET-25`, expiry at the second MIRROR_RETROSPECTIVE | § P6 |
| PID-08 | EVENT LEDGER design (a): per-actor append-only JSONL, consolidated by Plan | J.1 | Option (b) derives events from commits and messages, and cannot represent `CHECKPOINT_WRITTEN`, `ACTOR_DOWN`, `LEASE_STALE` or `RESUMED_FROM_MILESTONE`. A ledger that cannot record its own recovery events is the wrong instrument for Mirror. | PROPOSED | § P7 — **design only, no writer** |
| PID-09 | One shared `roles/scientist.md` instead of three `<suo>.md` files | I.2 step 7 vs body §32 | §32 makes the three equivalent; three identical files fork on first edit, and one contract gives them a single `ROLE_CONTRACT_HASH`, which is what makes their fingerprints comparable. **Declared deviation from the literal path.** | PROPOSED | `roles/scientist.md` |
| PID-10 | The §35.2 common section is a pointer table, not a paste, in each role contract | body §35.2 | `governance/` is tracked and therefore already present in every worktree; pasting creates six copies that drift. | PROPOSED | `roles/*.md` |
| PID-11 | `deployment/local_instance.md` untracked; the profile holds only the portable half | I.5 | An absolute path in the tracked half breaks the clone-and-run test I.5 exists to protect, and in a public repository it also carries a username the release gate does not look for. | PROPOSED | `deployment/deployment_profile.md`, `.gitignore` |
| PID-12 | Proposed ACTOR_IDs `scientist-a/b/c` | I.2 step 7, I.4 | `orchestrator` is fixed by I.3 literally, `plan` was assigned, `mirror` follows; the scientists' worktrees are `lettore*`, so the identifier does not follow from the directory. An ACTOR_ID is permanent and carries provenance. | **UNRESOLVED** — confirmed at registration, not by materialization | `BOOTSTRAP.md`, `deployment_profile.md` |
| PID-13 | Checkpoint and ledger paths: `ledger/checkpoints/<ACTOR_ID>/`, `ledger/events/<ACTOR_ID>.jsonl` | A.6, J.1 (paths unspecified) | Keeps runtime state in one top-level tree rather than scattering it, and mirrors the per-actor one-writer shape chosen in PID-08. | PROPOSED | `ledger/checkpoints/plan/CHK-plan-0001.json` |
| PID-14 | Growth principle relocated to a **new** master file rather than an existing one | migration step 2.2 | Three inventories found no file holding it; `framework/master/` is the existing layer for superordinate principles, so this is a new file in an existing home, not a new home. | PROPOSED | `framework/master/designed_for_growth.md` |
| PID-15 | Steps 2 and 4 executed in reverse order | instruction ordering vs non-loss | `main` had added an operating rule the old base lacked; migrating first would have dropped it. Non-loss is stated as non-negotiable, so sequence yielded to it. | PROPOSED | MAT-006 |
| PID-16 | `MIRROR_RETROSPECTIVE` interval `N` not chosen | G.3 | The annexes assign it to nobody, and G.2 places retrospective methodology inside Mirror's own method, which Mirror may not change alone. Plan did not take it. | **UNRESOLVED** | — |

---

## 3 · CLAUDE.md MIGRATION MAP

The complete rule-by-rule map is
[`governance/design_records/claude_md_migration_map.md`](../design_records/claude_md_migration_map.md),
part of this candidate at commit `04cbd3bd`.

**Declared deviation:** the map is referenced, not pasted into this manifest. Reproducing a
hundred-row table in two tracked files creates exactly the second-source-of-truth condition
recorded twice already as a session learning (MAT-003, MAT-004); the map is inside the candidate
and inside the hash, so a reviewer reads one authoritative copy. Summary of what it contains:

| Group | PRESERVED (moved) | REPLACED_BY_EQUIVALENT | UNRESOLVED |
|---|---|---|---|
| A · Parity of sources | 8 | 4 | 0 |
| B · Growth principle | 10 | 0 | 0 |
| C · Epistemic discipline | 2 | 6 | 0 |
| D · Operational and commit rules | 12 | 10 | 0 |
| E · Inbound pointers | 4 | 0 | 2 |

The router is 107 lines and legislates nothing. Every pointer in it resolves (verified
mechanically). Content that existed **only** in the old file — the whole growth principle, parity
rules 5c and 5e, `MODE: Q&A`, the one-actor-one-worktree section, the literal blocking strings,
the recovery chain — was moved in full, not summarised.

---

## 4 · PENDING_IMPLEMENTATION

Deliberately not built before review, per the frozen perimeter (instruction step 3).

| Component | Required by | Why pending | Eligible when |
|---|---|---|---|
| EVENT LEDGER writer + validator | J.1 | Design chosen (PID-08); building it is new development outside the candidate's needs. Must reuse the existing append-only receipt-ledger machinery, not a second mechanism. | After canonical commit, before the first scientific batch — Mirror needs it as its analysis surface |
| Consolidated event view | J.1 | Derived from the ledger; cannot precede it | With the writer |
| `LEARNING_INDEX` | E.2 | Schema now available; six session-learning records (MAT-001…006) are waiting to be filed into it | Immediately after canonical commit |
| `ACTIVE_LESSONS` role subsets | E.5, P6 | Depends on `LEARNING_INDEX` and on Mirror's clustering, which is Mirror's authority, not Plan's | After `LEARNING_INDEX` and a first Mirror clustering |
| Runtime / authority inventory + Agent Card registry | I.4, §43 | Rows would be fabricated: no actor has registered, no capability has been smoke-tested, and a stale row is explicitly non-authoritative | At bootstrap step 5, populated by registration and L2 |
| `deployment/local_instance.md` | I.5 | Machine-specific and untracked by design | At bootstrap step 2 |
| Scientist C worktree `lettore-c` | §32, §47 step 6 | Creating a worktree is a bootstrap act, not a materialization act | At bootstrap step 3 |
| Root `CLAUDE.md` → skills/README pointer repair | migration map § F.1 | Outside the frozen perimeter; mechanical but touches skill files | With Mirror's verdict on the migration |

---

## 5 · UNRESOLVED

Not hidden inside descriptive text.

1. **PID-12 — scientist ACTOR_IDs.** Proposed, permanent once set, confirmed at registration.
2. **PID-16 — `MIRROR_RETROSPECTIVE` interval `N`.** Assigned by the annexes to nobody.
3. ~~The prior-art review design record has never been transmitted.~~ **FULLY CLOSED at revision
   4** (MAT-009, MAT-010): transmitted, then archived byte-faithfully from the filesystem source
   with `BYTE_IDENTITY: PASS`. The revision-3 transcription caveat is closed with it.
4. **Two skill anchors point at `CLAUDE.md` sections that no longer exist** —
   `legend-start` cites *§LINT Severity* and *§Recovery*, both now in `LEGEND_CORE.md` §21b.
   Two descriptive lines in `ARCHITECTURE.md` and `README.md` still call `CLAUDE.md` the
   normative bootstrap.
5. **Rule 5c's `extraction_method` declaration has no schema field.** Now stated canonically and
   enforced by nothing.
6. **`framework/master/` holds two peer principles.** Whether that is the right shape is a
   structural judgement for Mirror, not for Plan alone.
7. **The operator's home path appears in 11 tracked files**, one of them a Plan record.
   Pre-existing practice; the publication gate passes; flagged once.
8. ~~No fingerprint is load-bearing until an actor other than Plan has run the composer.~~
   **DISCHARGED at revision 5** — Mirror independently ran `governance_fingerprint.py` and
   reproduced `c1d1a9cf…7ccc81da` exactly (NBN-1). The composer is deterministic across actors
   and PID-03's parse-the-prose design works in another actor's hands. **Bounded:** that
   validates the *tool* under the bytes as they were; RC-2 changed `plan_defined_parameters.md`,
   so the four current fingerprint *values* are again single-actor and await reproduction.
9. **The four post-RC-2 fingerprints have not been independently reproduced** — see above. This
   is a new item, not a reopening of #8.
10. **Every existing checkpoint is now unusable for resume.** `CHK-plan-0001…0005` record the
    pre-RC-2 fingerprint; `CHK-plan-0006` opens generation 2. This is P2.3's intended behaviour
    on a `CORE` change and Mirror's ESC-1 first data point, not a defect — recorded so the
    invalidation is visible rather than inferred.

---

## 6 · Verification record

| Check | Command | Result |
|---|---|---|
## 5b · Mirror REQUIRED_CHANGES — status

Source of truth: `c6a290e1` · `reviews/mirror/REV-GOV311-MIRROR-001.md`, read in full from the
durable state rather than from a summary.

| RC | Status | Resolution |
|---|---|---|
| **RC-1** binding not reproducible | **RESOLVED** | The mechanism is stated, not inferred: the recorded value came from `LISTING=$(…)`, where command substitution strips the trailing newline, while the published command used a pipe that keeps it. Both were re-executed at `3c6c6e15` and each returns its own value — `2e7da13f…` and `e3ca1983…`, one byte apart. This satisfies Mirror's `WHAT_WOULD_CHANGE_MY_MIND` as a *diagnosis*; it does not revive revision 4, which was unverifiable by a third party and is superseded. |
| **RC-2** P5 and manifest contradictory | **RESOLVED** | P5 rewritten as the single authoritative definition (version, control-plane roots, byte layout, reproduction command). The manifest references it. `candidate_content_hash.py` parses P5 instead of copying it. Declared as a `CORE` change with its full cascade — see PREVIOUS_CHECKPOINT_COMPATIBILITY. |
| **RC-3** entry count stale | **RESOLVED** | The count is no longer written in prose. `--show-domain` derives it from the same pass that builds the hash, so it cannot be stale beside the value. |
| **RC-4** `TIP` stale/ambiguous | **RESOLVED** | `TIP` removed. `BRANCH_TIP` (defines the domain), `MANIFEST_COMMIT` (control plane, outside it) and `SOURCE_COMMITS` are distinct and mechanically checkable. Their equality of hash is now a property of the definition, not a coincidence. |
| **RC-5** checkpoint fixed point | **RESOLVED** | Resolved by classification, not exception: PID-19 / P5.1. Checkpoints, task claims and the event ledger are control plane; the exclusion is rule-based, exhaustively declared, auditable via `--show-domain`, and cannot swallow anything scientific or normative. |

### Reproduction, executed by the author from the repository root

```
REPRODUCTION_COMMAND  python3 governance/scripts/candidate_content_hash.py \
                        --base 749a9a9b8f29c855f803a43b979c591532557561 \
                        --tip  9720a0cd1dddfe457f8e4eec1ebe47bd627512cc
EXPECTED              c39ecae89677363802c8c7d24b704da185fc568fed360b08ad01adb39730c239
OBTAINED (run 1)      c39ecae89677363802c8c7d24b704da185fc568fed360b08ad01adb39730c239
OBTAINED (run 2)      c39ecae89677363802c8c7d24b704da185fc568fed360b08ad01adb39730c239
DOMAIN                504 included, 8 excluded (1 manifest + 6 checkpoints + 1 task claim)
```

**CONTROL_POSITIVE.** A negative comparison alone cannot distinguish the author's error from the
tool's — the lesson Mirror contributed in §1.2 of its review. The same shell that produced this
revision reproduces a *known-good* value: revision 1's superseded hash, under the P5 v1 recipe it
was computed with, returns `f869a5237a70634ada535343b7484ddc45f36111e9c79664179086a81f8b8909`
exactly. The tooling is therefore validated positively before its negative results are relied on.

---

## 6 · Verification record

*Re-run at revision 5, after the Mirror remediation.*

| Check | Command | Result |
|---|---|---|
| Structural LINT | `python3 framework/scripts/legend_lint.py .` | `VERDICT: PASS` (1 INFO: CLAIM 010 background-only, wikilink not required) |
| Publication gate | `python3 scripts/public_release_gate.py` | `VERDICT: PASS`, `BLOCKS: 0`; 4 pre-existing non-blocking `[REVIEW]` items on parent-of-origin attribution, unrelated to this candidate |
| Fingerprints | `python3 governance/scripts/governance_fingerprint.py compose --all` | all four **changed** — RC-2 amended a CORE artifact. plan `37c3b863…`, mirror `84d2b841…`, orchestrator `6b55605d…`, scientist `ce3c0d94…` |
| Checkpoint compatibility | recorded in `CHK-plan-0006` | `CHK-plan-0001…0005` INCOMPATIBLE (pre-RC-2 fingerprint, and a different task and generation); none used to resume; none rewritten |
| Candidate hash determinism | the published command, twice | identical; and unchanged after the manifest commit, since the manifest is control plane |
| Rule 4 is a true relocation | `diff` of the destination text against `749a9a9:CLAUDE.md` | identical (whitespace-normalised) after the PID-17 withdrawal |
| Router pointers | existence check over every path and skill named in `CLAUDE.md` | all resolve |
| Portability | grep for absolute paths in `governance/`, `roles/`, `BOOTSTRAP.md`, `deployment/` | none outside `design_records/` |
| Base condition | `git rev-parse main` vs recorded `BASE_HEAD` | identical; branch 0 behind, 12 ahead — **no re-alignment needed** |
| Git state | `git status` | clean; no foreign file staged |
| Design record byte identity | `shasum -a 256` on source and archive; `cmp` | `2563f82e…4d6988e` both sides, `cmp` clean → **`BYTE_IDENTITY: PASS`** |
| Design record content | em dash, accented letters, `✔` ×7, `⚠` ×3, `Agno — NON verificato indipendentemente`, all ten `E1`–`E10` headings, DEFER register, `PARTE 5` verdict, zero mojibake | all PASS — additional to the hash, which is the primary proof |

`LINT_RESULT = PASS` and `PUBLICATION_GATE = PASS / BLOCKS: 0` are met, so the candidate is
`READY FOR MIRROR HOSTILE REVIEW`.

**NO CANONICAL_BATCH_COMMIT EXECUTED.** Plan does not hold that authority and did not simulate it.
