# Follow-up tasks from Mirror REV-EXPOST-20260911-001 — queued, not improvised

**Recorded by:** `orchestrator`, 2026-09-11 · Under §21e a Mirror finding is a new task, never a
hold on a landed change. Four of the review's findings were repaired by the coordinator the same
morning (F3 wrapper directory refusal, F2's measurement half, F5 census arithmetic, F1c untracked
creations — see the review's `AUTHOR_RESPONSE`). The rest are design decisions or belong to files
whose stream has closed, and are queued here with acceptance criteria so the next `plan` session
starts from the criterion, not from the review.

| ID | Finding | Task | Acceptance |
|---|---|---|---|
| MF-1 | F1(d) — the `--guarded` tracer prevents through Python `open` and six `os.*` calls only; `sh -c "echo … >"` and `os.open` land bytes; `fulltext_receipts.py` and `task_close.py` write through `os.open`; three tools issue git commands that write the working tree | Decide the guard's honest boundary and enforce it: either wrap `os.open` / `os.replace` / `subprocess` under `--guarded` and refuse git working-tree writes under a guarded root, or state in the meta-test's verdict that a suite driving `os.open`, a shell or git is `UNGUARDABLE` and count it as such | Mirror's F1(d) fixture (`sh -c` echo, `os.open` truncate) refused or reported `UNGUARDABLE` — never green; mutation restoring the hole turns red |
| MF-2 | F1(e) — only `mutate_matrix.py` goes through `--guarded`, by convention; a scientist's direct suite run does not | Make the refusal the default for any suite run from `framework/scripts/` when a guarded tree is writable — a `sitecustomize` on the runner's `PYTHONPATH` — or record in the brief that direct suite runs are unguarded | `python3 test_x.py` from the checkout with a planted write mutant refuses, or the brief says in one sentence that it does not |
| MF-3 | F2 — `deepdive_manifest.py` does not know `contradicts_locator`; a silent in-place reversal of a persisted locator passes every gate | (a) validator branch: `contradicts_locator` must be an object with `manifest`, `entry`, `what_changed`; a declared contradiction without an `audit` object is a `[DECLARED GAP]` that **blocks a complete receipt** (it is the R4 floor); (b) a diff-based detector: persisted locator text changed between two committed revisions of the same entry index with no declaration → `UNDECLARED_CONTRADICTION` in `locator_contradiction_audit.py` | Mirror's F2 fixture (reversed `entries[0]`, string declaration) fails the strict validator; the diff detector names the entry from `git log -p`; G1/G2-style mutations red |
| MF-4 | F4 — `retraction_check.dependencies` accepts another paper's block | Bind the block to its manifest: `references_declared` must equal `multihop.references_enumerated`, and the block carries `reference_source` naming this PMID/DOI, refused on mismatch | Mirror's F4 fixture (18460020's block in 16223882) refused; matrix row added |
| MF-5 | F6 — `external_provenance.date` checks shape, not calendar (`2026-13-45` passes) | `datetime.date.fromisoformat` in `external_provenance_defect` | the literal `2026-13-45` refused; a real date passes |
| MF-6 | F6 — `genre_discriminator` and `erratum_scope_check` exit 0 on `INSUFFICIENT_DATA` / nothing examined | exit code 2 on an uninformative verdict, matching the other five screens | `--artifact <empty>` and `--pmid <absent>` exit non-zero; the verdict record unchanged |
| MF-7 | F7 — `HARNESS-QUOTA-RESUME-001` wrote `~/.claude/settings.json` under a `null` write perimeter | Operator's own `plan` session: a `WRITE_PERIMETER` line naming the user-level file, and a `DECISIONS_TAKEN` entry naming reversibility (the backup and revert instruction already exist) | the ledger carries both; nothing else changes |
| MF-8 | P3 — the "4 undeclared containers" is a file count; the 4 are still undeclared | The next scientist wave declares the four `.pptx` containers on PMID 38499540 and 33916893 as `supplement_binary`, as a reading act by the readers of those papers | `evidence_presence.py` artefact count +4, `digest_mismatch` 0 |
